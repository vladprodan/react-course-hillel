# Рішення: useTransition для оптимізованого пошуку

## Крок 1: Створення утиліт для генерації даних

Створіть файл `dataGenerator.js` у папці `src/utils`:

```javascript
// Генерація великого масиву даних для тестування продуктивності

// Функція генерації одного продукту
const generateProduct = (id) => ({
  id,
  name: `Продукт ${id}`,
  category: ["Електроніка", "Одяг", "Книги", "Побутова техніка", "Спорт"][
    id % 5
  ],
  price: Math.floor(Math.random() * 1000) + 10,
  description: `Опис для продукту номер ${id}. Це якісний товар за доступною ціною.`,
});

// Генерація масиву продуктів
export const generateProducts = (count = 10000) => {
  console.log(`🔄 Генерація ${count} продуктів...`);
  const products = [];

  for (let i = 1; i <= count; i++) {
    products.push(generateProduct(i));
  }

  console.log(`✅ Згенеровано ${count} продуктів`);
  return products;
};

// "Важка" функція фільтрації для імітації складних обчислень
export const filterProducts = (query, allProducts) => {
  if (!query || query.trim() === "") {
    return allProducts.slice(0, 100); // Показуємо перші 100 без пошуку
  }

  const startTime = performance.now();
  console.log(`🔍 Початок фільтрації для запиту: "${query}"`);

  const lowerQuery = query.toLowerCase();

  // Імітація "важких" обчислень
  // Додаємо штучну затримку для кожного елемента
  const filtered = allProducts.filter((product) => {
    // Штучне ускладнення обчислень
    for (let i = 0; i < 100; i++) {
      Math.sqrt(i);
    }

    return (
      product.name.toLowerCase().includes(lowerQuery) ||
      product.category.toLowerCase().includes(lowerQuery) ||
      product.description.toLowerCase().includes(lowerQuery)
    );
  });

  const endTime = performance.now();
  console.log(
    `✅ Фільтрація завершена за ${(endTime - startTime).toFixed(2)}мс`
  );
  console.log(`📊 Знайдено ${filtered.length} результатів`);

  return filtered;
};

// Альтернативна "супер важка" фільтрація для тестування
export const heavyFilterProducts = (query, allProducts) => {
  if (!query || query.trim() === "") {
    return allProducts.slice(0, 100);
  }

  const startTime = performance.now();
  const lowerQuery = query.toLowerCase();

  const filtered = allProducts.filter((product) => {
    // Дуже важкі обчислення
    for (let i = 0; i < 1000; i++) {
      Math.sqrt(i) * Math.random();
    }

    return product.name.toLowerCase().includes(lowerQuery);
  });

  const endTime = performance.now();
  console.log(`⚠️ Важка фільтрація за ${(endTime - startTime).toFixed(2)}мс`);

  return filtered;
};
```

---

## Крок 2: Створення оптимізованого компонента пошуку

Створіть файл `OptimizedSearch.jsx` у папці `src/components`:

```jsx
import React, { useState, useTransition, useMemo } from "react";
import { generateProducts, filterProducts } from "../utils/dataGenerator";

const OptimizedSearch = () => {
  // Генерація даних один раз при монтуванні
  const allProducts = useMemo(() => generateProducts(10000), []);

  // Терміновий стан - для інпуту (оновлюється миттєво)
  const [query, setQuery] = useState("");

  // Нетерміновий стан - для результатів (оновлюється з затримкою)
  const [filteredResults, setFilteredResults] = useState(
    allProducts.slice(0, 100)
  );

  // Ініціалізація useTransition
  const [isPending, startTransition] = useTransition();

  // Обробник пошуку
  const handleSearch = (value) => {
    // 1. ТЕРМІНОВЕ оновлення - інпут реагує миттєво
    setQuery(value);

    // 2. НЕТЕРМІНОВЕ оновлення - фільтрація може почекати
    startTransition(() => {
      const results = filterProducts(value, allProducts);
      setFilteredResults(results);
    });
  };

  return (
    <div className="optimized-search-container">
      <div className="search-card">
        <h2>🔍 Оптимізований Пошук</h2>
        <p className="search-subtitle">useTransition для плавного UX</p>

        {/* Поле пошуку */}
        <div className="search-input-section">
          <div className="search-input-wrapper">
            <span className="search-icon">🔍</span>
            <input
              type="text"
              value={query}
              onChange={(e) => handleSearch(e.target.value)}
              placeholder="Пошук серед 10,000 продуктів..."
              className="search-input"
            />
            {query && (
              <button onClick={() => handleSearch("")} className="clear-search">
                ✕
              </button>
            )}
          </div>

          {/* Індикатор стану */}
          <div className="search-status">
            {isPending ? (
              <span className="status-pending">
                <span className="spinner-tiny"></span>
                Фільтрація...
              </span>
            ) : (
              <span className="status-ready">
                ✓ Готово ({filteredResults.length} результатів)
              </span>
            )}
          </div>
        </div>

        {/* Результати пошуку */}
        <div
          className={`results-container ${isPending ? "results-loading" : ""}`}
        >
          {filteredResults.length > 0 ? (
            <div className="products-grid-search">
              {filteredResults.map((product) => (
                <div key={product.id} className="product-card-mini">
                  <div className="product-header-mini">
                    <h4>{product.name}</h4>
                    <span className="product-category">{product.category}</span>
                  </div>
                  <p className="product-price-mini">{product.price} грн</p>
                </div>
              ))}
            </div>
          ) : (
            <div className="no-results">
              <p>😔 Нічого не знайдено</p>
              <span>Спробуйте інший запит</span>
            </div>
          )}
        </div>

        {/* Інформаційний блок */}
        <div className="transition-info">
          <h4>⚡ Як працює useTransition:</h4>
          <div className="info-grid">
            <div className="info-item">
              <strong>Терміновий стан (query):</strong>
              <p>Оновлюється миттєво при введенні</p>
              <code>setQuery(value)</code>
            </div>
            <div className="info-item">
              <strong>Нетерміновий стан (results):</strong>
              <p>Оновлюється з низьким пріоритетом</p>
              <code>startTransition(() =&gt; ...)</code>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default OptimizedSearch;
```

---

## Крок 3: Створення порівняльного компонента (без useTransition)

Створіть файл `NonOptimizedSearch.jsx` у папці `src/components`:

```jsx
import React, { useState, useMemo } from "react";
import { generateProducts, filterProducts } from "../utils/dataGenerator";

const NonOptimizedSearch = () => {
  const allProducts = useMemo(() => generateProducts(10000), []);

  // Тільки один стан - це призводить до блокування UI
  const [query, setQuery] = useState("");
  const [filteredResults, setFilteredResults] = useState(
    allProducts.slice(0, 100)
  );

  // Обробник БЕЗ useTransition - UI блокується
  const handleSearch = (value) => {
    setQuery(value);

    // ❌ Синхронне оновлення - блокує UI
    const results = filterProducts(value, allProducts);
    setFilteredResults(results);
  };

  return (
    <div className="optimized-search-container">
      <div className="search-card warning">
        <h2>⚠️ Неоптимізований Пошук</h2>
        <p className="search-subtitle">
          Без useTransition - UI може "заморожуватися"
        </p>

        {/* Поле пошуку */}
        <div className="search-input-section">
          <div className="search-input-wrapper">
            <span className="search-icon">🔍</span>
            <input
              type="text"
              value={query}
              onChange={(e) => handleSearch(e.target.value)}
              placeholder="Спробуйте швидко друкувати..."
              className="search-input"
            />
          </div>

          <div className="search-status">
            <span className="status-ready">
              {filteredResults.length} результатів
            </span>
          </div>
        </div>

        {/* Результати */}
        <div className="results-container">
          {filteredResults.length > 0 ? (
            <div className="products-grid-search">
              {filteredResults.map((product) => (
                <div key={product.id} className="product-card-mini">
                  <div className="product-header-mini">
                    <h4>{product.name}</h4>
                    <span className="product-category">{product.category}</span>
                  </div>
                  <p className="product-price-mini">{product.price} грн</p>
                </div>
              ))}
            </div>
          ) : (
            <div className="no-results">
              <p>😔 Нічого не знайдено</p>
            </div>
          )}
        </div>

        {/* Попередження */}
        <div className="warning-box">
          <h4>⚠️ Проблеми без useTransition:</h4>
          <ul>
            <li>Інпут може "підвисати" при швидкому введенні</li>
            <li>UI блокується під час фільтрації</li>
            <li>Погана користувацька взаємодія</li>
            <li>Неможливо відмінити попередню операцію</li>
          </ul>
        </div>
      </div>
    </div>
  );
};

export default NonOptimizedSearch;
```

---

## Крок 4: Створення розширеного прикладу з статистикою

Створіть файл `AdvancedOptimizedSearch.jsx`:

```jsx
import React, { useState, useTransition, useMemo, useEffect } from "react";
import { generateProducts, filterProducts } from "../utils/dataGenerator";

const AdvancedOptimizedSearch = () => {
  const allProducts = useMemo(() => generateProducts(20000), []);

  const [query, setQuery] = useState("");
  const [filteredResults, setFilteredResults] = useState([]);
  const [isPending, startTransition] = useTransition();

  // Статистика продуктивності
  const [stats, setStats] = useState({
    totalSearches: 0,
    averageTime: 0,
    lastSearchTime: 0,
  });

  // Спостереження за продуктивністю
  const [inputDelay, setInputDelay] = useState(0);
  const [lastInputTime, setLastInputTime] = useState(Date.now());

  useEffect(() => {
    setFilteredResults(allProducts.slice(0, 100));
  }, [allProducts]);

  const handleSearch = (value) => {
    // Вимірювання затримки інпуту
    const now = Date.now();
    const delay = now - lastInputTime;
    setInputDelay(delay);
    setLastInputTime(now);

    setQuery(value);

    startTransition(() => {
      const startTime = performance.now();
      const results = filterProducts(value, allProducts);
      const endTime = performance.now();
      const searchTime = endTime - startTime;

      setFilteredResults(results);

      // Оновлення статистики
      setStats((prev) => ({
        totalSearches: prev.totalSearches + 1,
        averageTime:
          (prev.averageTime * prev.totalSearches + searchTime) /
          (prev.totalSearches + 1),
        lastSearchTime: searchTime,
      }));
    });
  };

  return (
    <div className="optimized-search-container">
      <div className="search-card advanced">
        <h2>🚀 Розширений Оптимізований Пошук</h2>
        <p className="search-subtitle">
          20,000 продуктів + статистика продуктивності
        </p>

        {/* Поле пошуку */}
        <div className="search-input-section">
          <div className="search-input-wrapper">
            <span className="search-icon">🔍</span>
            <input
              type="text"
              value={query}
              onChange={(e) => handleSearch(e.target.value)}
              placeholder="Пошук серед 20,000 продуктів..."
              className="search-input"
            />
            {query && (
              <button onClick={() => handleSearch("")} className="clear-search">
                ✕
              </button>
            )}
          </div>

          <div className="search-status">
            {isPending ? (
              <span className="status-pending">
                <span className="spinner-tiny"></span>
                Фільтрація...
              </span>
            ) : (
              <span className="status-ready">
                ✓ {filteredResults.length} результатів
              </span>
            )}
          </div>
        </div>

        {/* Статистика продуктивності */}
        <div className="performance-stats">
          <h4>📊 Статистика продуктивності:</h4>
          <div className="stats-grid">
            <div className="stat-box">
              <span className="stat-label">Затримка інпуту:</span>
              <span
                className={`stat-value ${inputDelay < 50 ? "good" : "bad"}`}
              >
                {inputDelay}мс
              </span>
            </div>
            <div className="stat-box">
              <span className="stat-label">Останній пошук:</span>
              <span className="stat-value">
                {stats.lastSearchTime.toFixed(2)}мс
              </span>
            </div>
            <div className="stat-box">
              <span className="stat-label">Середній час:</span>
              <span className="stat-value">
                {stats.averageTime.toFixed(2)}мс
              </span>
            </div>
            <div className="stat-box">
              <span className="stat-label">Всього пошуків:</span>
              <span className="stat-value">{stats.totalSearches}</span>
            </div>
          </div>
          <p className="performance-note">
            {inputDelay < 50
              ? "✅ Інпут реагує миттєво завдяки useTransition!"
              : "⚠️ Виявлено затримку інпуту"}
          </p>
        </div>

        {/* Результати */}
        <div
          className={`results-container ${isPending ? "results-loading" : ""}`}
        >
          <div className="results-header">
            <h3>Результати пошуку</h3>
            {isPending && <div className="loading-overlay">Оновлення...</div>}
          </div>

          {filteredResults.length > 0 ? (
            <div className="products-grid-search">
              {filteredResults.slice(0, 50).map((product) => (
                <div key={product.id} className="product-card-mini">
                  <div className="product-header-mini">
                    <h4>{product.name}</h4>
                    <span className="product-category">{product.category}</span>
                  </div>
                  <p className="product-description-mini">
                    {product.description}
                  </p>
                  <p className="product-price-mini">{product.price} грн</p>
                </div>
              ))}
            </div>
          ) : (
            <div className="no-results">
              <p>😔 Нічого не знайдено</p>
            </div>
          )}

          {filteredResults.length > 50 && (
            <p className="results-note">
              Показано перші 50 з {filteredResults.length} результатів
            </p>
          )}
        </div>
      </div>
    </div>
  );
};

export default AdvancedOptimizedSearch;
```

---

## Крок 5: Додавання стилів

Додайте до `App.css`:

```css
/* Контейнер пошуку */
.optimized-search-container {
  padding: 40px 20px;
  max-width: 1400px;
  margin: 0 auto;
}

.search-card {
  background: white;
  border-radius: 20px;
  padding: 40px;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
}

.search-card.warning {
  border: 3px solid #ff9800;
}

.search-card.advanced {
  max-width: 1200px;
  margin: 0 auto;
}

.search-subtitle {
  color: #666;
  text-align: center;
  margin-bottom: 30px;
}

/* Секція інпуту */
.search-input-section {
  margin-bottom: 30px;
}

.search-input-wrapper {
  position: relative;
  display: flex;
  align-items: center;
  background: #f5f5f5;
  border-radius: 12px;
  padding: 0 20px;
  transition: all 0.3s ease;
}

.search-input-wrapper:focus-within {
  background: white;
  box-shadow: 0 4px 12px rgba(102, 126, 234, 0.2);
}

.search-icon {
  font-size: 24px;
  margin-right: 12px;
  color: #999;
}

.search-input {
  flex: 1;
  border: none;
  background: transparent;
  padding: 18px 0;
  font-size: 16px;
  outline: none;
  color: #333;
}

.clear-search {
  background: none;
  border: none;
  color: #999;
  font-size: 20px;
  cursor: pointer;
  padding: 5px;
  transition: color 0.3s ease;
}

.clear-search:hover {
  color: #333;
}

/* Статус пошуку */
.search-status {
  margin-top: 15px;
  display: flex;
  justify-content: center;
  gap: 10px;
}

.status-pending,
.status-ready {
  padding: 8px 16px;
  border-radius: 20px;
  font-size: 14px;
  font-weight: 600;
  display: flex;
  align-items: center;
  gap: 8px;
}

.status-pending {
  background: #fff3e0;
  color: #ef6c00;
}

.status-ready {
  background: #e8f5e9;
  color: #2e7d32;
}

.spinner-tiny {
  width: 12px;
  height: 12px;
  border: 2px solid rgba(239, 108, 0, 0.3);
  border-top-color: #ef6c00;
  border-radius: 50%;
  animation: spin 0.6s linear infinite;
}

/* Контейнер результатів */
.results-container {
  transition: opacity 0.3s ease;
  position: relative;
}

.results-container.results-loading {
  opacity: 0.6;
}

.results-header {
  position: relative;
  margin-bottom: 20px;
}

.loading-overlay {
  position: absolute;
  top: 0;
  right: 0;
  background: #ff9800;
  color: white;
  padding: 4px 12px;
  border-radius: 12px;
  font-size: 12px;
  font-weight: 600;
  animation: pulse 1.5s ease-in-out infinite;
}

/* Сітка продуктів */
.products-grid-search {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 20px;
}

.product-card-mini {
  background: linear-gradient(135deg, #667eea05 0%, #764ba205 100%);
  border: 2px solid #e0e0e0;
  border-radius: 12px;
  padding: 20px;
  transition: all 0.3s ease;
}

.product-card-mini:hover {
  transform: translateY(-5px);
  box-shadow: 0 6px 20px rgba(102, 126, 234, 0.15);
  border-color: #667eea;
}

.product-header-mini {
  display: flex;
  justify-content: space-between;
  align-items: start;
  gap: 10px;
  margin-bottom: 10px;
}

.product-header-mini h4 {
  color: #333;
  margin: 0;
  font-size: 16px;
  flex: 1;
}

.product-category {
  background: #f5f5f5;
  color: #666;
  padding: 4px 10px;
  border-radius: 12px;
  font-size: 11px;
  white-space: nowrap;
  font-weight: 600;
}

.product-description-mini {
  color: #666;
  font-size: 13px;
  margin: 10px 0;
  line-height: 1.5;
}

.product-price-mini {
  color: #667eea;
  font-size: 18px;
  font-weight: bold;
  margin: 0;
}

/* Порожні результати */
.no-results {
  text-align: center;
  padding: 60px 20px;
}

.no-results p {
  font-size: 24px;
  color: #999;
  margin-bottom: 10px;
}

.no-results span {
  color: #666;
  font-size: 14px;
}

.results-note {
  text-align: center;
  color: #999;
  font-size: 14px;
  margin-top: 20px;
  font-style: italic;
}

/* Інформаційний блок */
.transition-info {
  margin-top: 30px;
  padding: 25px;
  background: linear-gradient(135deg, #667eea10 0%, #764ba210 100%);
  border-radius: 12px;
  border: 2px solid #667eea30;
}

.transition-info h4 {
  color: #333;
  margin-bottom: 20px;
  font-size: 18px;
}

.info-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 20px;
}

.info-item {
  background: white;
  padding: 20px;
  border-radius: 10px;
  border: 2px solid #e0e0e0;
}

.info-item strong {
  color: #667eea;
  display: block;
  margin-bottom: 10px;
  font-size: 15px;
}

.info-item p {
  color: #666;
  font-size: 14px;
  margin-bottom: 10px;
}

.info-item code {
  background: #f5f5f5;
  padding: 4px 8px;
  border-radius: 4px;
  font-size: 12px;
  color: #667eea;
  display: inline-block;
}

/* Попередження */
.warning-box {
  margin-top: 25px;
  padding: 20px;
  background: #fff3e0;
  border-radius: 10px;
  border-left: 4px solid #ff9800;
}

.warning-box h4 {
  color: #333;
  margin-bottom: 15px;
  font-size: 16px;
}

.warning-box ul {
  list-style: none;
  padding: 0;
  margin: 0;
}

.warning-box li {
  color: #666;
  font-size: 14px;
  padding: 8px 0;
  padding-left: 25px;
  position: relative;
}

.warning-box li::before {
  content: "⚠️";
  position: absolute;
  left: 0;
}

/* Статистика продуктивності */
.performance-stats {
  margin: 30px 0;
  padding: 25px;
  background: #f9f9f9;
  border-radius: 12px;
}

.performance-stats h4 {
  color: #333;
  margin-bottom: 20px;
  font-size: 18px;
}

.stats-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: 15px;
  margin-bottom: 15px;
}

.stat-box {
  background: white;
  padding: 15px;
  border-radius: 10px;
  border: 2px solid #e0e0e0;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.stat-label {
  font-size: 12px;
  color: #999;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.stat-value {
  font-size: 24px;
  font-weight: bold;
  color: #333;
}

.stat-value.good {
  color: #4caf50;
}

.stat-value.bad {
  color: #f44336;
}

.performance-note {
  text-align: center;
  color: #666;
  font-size: 14px;
  font-weight: 600;
  margin: 0;
  padding: 12px;
  background: white;
  border-radius: 8px;
}

/* Адаптивність */
@media (max-width: 768px) {
  .products-grid-search {
    grid-template-columns: 1fr;
  }

  .info-grid {
    grid-template-columns: 1fr;
  }

  .stats-grid {
    grid-template-columns: 1fr 1fr;
  }
}
```

---

## Крок 6: Інтеграція в App.jsx

```jsx
import OptimizedSearch from './components/OptimizedSearch';
import NonOptimizedSearch from './components/NonOptimizedSearch';
import AdvancedOptimizedSearch from './components/AdvancedOptimizedSearch';

// В Routes:
<Route path="/search-optimized" element={<OptimizedSearch />} />
<Route path="/search-not-optimized" element={<NonOptimizedSearch />} />
<Route path="/search-advanced" element={<AdvancedOptimizedSearch />} />
```

---

## Крок 7: Оновлення Navigation.jsx

```jsx
<li>
  <NavLink
    to="/search-optimized"
    className={({ isActive }) => isActive ? 'active-link' : 'default-link'}
  >
    ⚡ Оптимізований пошук
  </NavLink>
</li>
<li>
  <NavLink
    to="/search-not-optimized"
    className={({ isActive }) => isActive ? 'active-link' : 'default-link'}
  >
    ⚠️ Без оптимізації
  </NavLink>
</li>
<li>
  <NavLink
    to="/search-advanced"
    className={({ isActive }) => isActive ? 'active-link' : 'default-link'}
  >
    🚀 Розширений пошук
  </NavLink>
</li>
```

---

## Крок 8: Тестування

```bash
npm run dev
```

### Сценарії тестування:

#### Тест 1: Порівняння з/без useTransition

1. Відкрийте `/search-optimized`
2. Швидко друкуйте "продукт 123"
3. **Очікується:** Інпут реагує миттєво
4. Відкрийте `/search-not-optimized`
5. Швидко друкуйте
6. **Очікується:** Інпут "підвисає"

#### Тест 2: Перевірка isPending

1. На оптимізованій сторінці введіть текст
2. **Очікується:** "Фільтрація..." під час обчислень
3. Результати з напівпрозорістю

#### Тест 3: Статистика продуктивності

1. Відкрийте `/search-advanced`
2. Введіть кілька запитів
3. **Очікується:**
   - Затримка інпуту < 50мс (зелений)
   - Час фільтрації ~100-500мс
   - Оновлення статистики

---

## Результат

✅ **Розподіл пріоритетів** - `query` терміновий, `filteredResults` нетерміновий  
✅ **Використання isPending** - індикатор завантаження та зміна стилю  
✅ **UX** - Інпут завжди чутливий, навіть при важких обчисленнях

---

## Висновок

**useTransition** дозволяє:

- ⚡ Миттєвий відгук UI
- 🎯 Розподіл пріоритетів оновлень
- 📊 Плавна анімація переходів
- 🚀 Краща користувацька взаємодія

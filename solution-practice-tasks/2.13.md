# Рішення: Використання Fragment у React

## Завдання 1: Компонент "Профіль користувача"

### Створіть файл `UserProfile.jsx`

```jsx
// UserProfile.jsx
import { Fragment } from "react";
import "./UserProfile.css";

export default function UserProfile() {
  const user = {
    name: "Олена Шевченко",
    avatar: "https://via.placeholder.com/150",
    email: "olena@example.com",
    phone: "+380 67 123 4567",
    bio: "Full-stack розробник. Люблю React та TypeScript.",
    skills: ["React", "TypeScript", "Node.js", "PostgreSQL"],
    socialLinks: [
      { platform: "GitHub", url: "https://github.com/...", icon: "🐙" },
      { platform: "LinkedIn", url: "https://linkedin.com/...", icon: "💼" },
      { platform: "Twitter", url: "https://twitter.com/...", icon: "🐦" },
    ],
  };

  return (
    <div className="profile-card">
      {/* Секція: Заголовок (аватар + ім'я) - Fragment */}
      <>
        <img src={user.avatar} alt={user.name} className="profile-avatar" />
        <h1 className="profile-name">{user.name}</h1>
      </>

      {/* Секція: Контактна інформація - Fragment */}
      <>
        <div className="contact-info">
          <p className="contact-item">
            <span className="contact-icon">📧</span>
            <span className="contact-text">{user.email}</span>
          </p>
          <p className="contact-item">
            <span className="contact-icon">📱</span>
            <span className="contact-text">{user.phone}</span>
          </p>
        </div>
      </>

      {/* Секція: Біографія */}
      <p className="profile-bio">{user.bio}</p>

      {/* Секція: Навички */}
      <div className="skills-section">
        <h2 className="section-title">Навички</h2>
        <ul className="skills-list">
          {user.skills.map((skill, index) => (
            <Fragment key={index}>
              <li className="skill-item">{skill}</li>
            </Fragment>
          ))}
        </ul>
      </div>

      {/* Секція: Соціальні мережі - Fragment */}
      <>
        <h2 className="section-title">Соціальні мережі</h2>
        <div className="social-links">
          {user.socialLinks.map((link, index) => (
            <Fragment key={index}>
              <a
                href={link.url}
                className="social-link"
                target="_blank"
                rel="noopener noreferrer"
              >
                <span className="social-icon">{link.icon}</span>
                <span className="social-platform">{link.platform}</span>
              </a>
            </Fragment>
          ))}
        </div>
      </>
    </div>
  );
}
```

### Створіть файл `UserProfile.css`

```css
/* UserProfile.css */
.profile-card {
  max-width: 500px;
  margin: 40px auto;
  padding: 40px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-radius: 20px;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
  color: #fff;
  text-align: center;
}

.profile-avatar {
  width: 150px;
  height: 150px;
  border-radius: 50%;
  border: 5px solid #fff;
  margin-bottom: 20px;
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
}

.profile-name {
  font-size: 32px;
  font-weight: bold;
  margin: 0 0 25px 0;
  color: #fff;
}

.contact-info {
  background-color: rgba(255, 255, 255, 0.1);
  border-radius: 12px;
  padding: 20px;
  margin-bottom: 25px;
}

.contact-item {
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 10px 0;
  font-size: 16px;
}

.contact-icon {
  font-size: 24px;
  margin-right: 12px;
}

.contact-text {
  color: rgba(255, 255, 255, 0.95);
}

.profile-bio {
  font-size: 16px;
  line-height: 1.6;
  margin-bottom: 30px;
  color: rgba(255, 255, 255, 0.9);
  font-style: italic;
}

.section-title {
  font-size: 24px;
  margin-bottom: 15px;
  color: #fff;
  font-weight: bold;
}

.skills-section {
  margin-bottom: 30px;
}

.skills-list {
  list-style: none;
  padding: 0;
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 10px;
}

.skill-item {
  background-color: rgba(255, 255, 255, 0.2);
  padding: 8px 16px;
  border-radius: 20px;
  font-size: 14px;
  font-weight: 500;
  color: #fff;
  border: 2px solid rgba(255, 255, 255, 0.3);
  transition: all 0.3s ease;
}

.skill-item:hover {
  background-color: rgba(255, 255, 255, 0.3);
  transform: translateY(-2px);
}

.social-links {
  display: flex;
  justify-content: center;
  gap: 15px;
  flex-wrap: wrap;
}

.social-link {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 12px 20px;
  background-color: rgba(255, 255, 255, 0.2);
  border: 2px solid rgba(255, 255, 255, 0.3);
  border-radius: 10px;
  text-decoration: none;
  color: #fff;
  font-weight: 500;
  transition: all 0.3s ease;
}

.social-link:hover {
  background-color: rgba(255, 255, 255, 0.3);
  transform: translateY(-3px);
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
}

.social-icon {
  font-size: 24px;
}

.social-platform {
  font-size: 14px;
}
```

---

## Завдання 2: Компонент "Меню ресторану"

### Створіть файл `RestaurantMenu.jsx`

```jsx
// RestaurantMenu.jsx
import { Fragment } from "react";
import "./RestaurantMenu.css";

export default function RestaurantMenu() {
  const menuData = [
    {
      categoryId: 1,
      categoryName: "Перші страви",
      items: [
        {
          id: 101,
          name: "Борщ український",
          price: 85,
          description: "З м'ясом та сметаною",
        },
        { id: 102, name: "Солянка", price: 95, description: "М'ясна збірна" },
      ],
    },
    {
      categoryId: 2,
      categoryName: "Основні страви",
      items: [
        {
          id: 201,
          name: "Курка по-київськи",
          price: 180,
          description: "З гарніром",
        },
        {
          id: 202,
          name: "Стейк з яловичини",
          price: 250,
          description: "Середньої прожарки",
        },
      ],
    },
    {
      categoryId: 3,
      categoryName: "Десерти",
      items: [
        {
          id: 301,
          name: "Тірамісу",
          price: 75,
          description: "Класичний італійський",
        },
        { id: 302, name: "Чізкейк", price: 70, description: "Нью-йоркський" },
      ],
    },
  ];

  return (
    <div className="menu">
      <h1 className="menu-title">Меню ресторану</h1>

      {menuData.map((category) => (
        <Fragment key={category.categoryId}>
          <div className="category">
            <h2 className="category-name">{category.categoryName}</h2>

            <div className="items-list">
              {category.items.map((item) => (
                <Fragment key={item.id}>
                  <div className="menu-item">
                    <div className="item-header">
                      <h3 className="item-name">{item.name}</h3>
                      <span className="item-price">{item.price} грн</span>
                    </div>
                    <p className="item-description">{item.description}</p>
                  </div>
                </Fragment>
              ))}
            </div>
          </div>
        </Fragment>
      ))}
    </div>
  );
}
```

### Створіть файл `RestaurantMenu.css`

```css
/* RestaurantMenu.css */
.menu {
  max-width: 900px;
  margin: 40px auto;
  padding: 40px;
  background-color: #fff;
  border-radius: 16px;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
}

.menu-title {
  text-align: center;
  font-size: 42px;
  color: #2c3e50;
  margin-bottom: 40px;
  font-weight: bold;
  border-bottom: 3px solid #e74c3c;
  padding-bottom: 20px;
}

.category {
  margin-bottom: 50px;
}

.category:last-child {
  margin-bottom: 0;
}

.category-name {
  font-size: 32px;
  color: #e74c3c;
  margin-bottom: 25px;
  font-weight: bold;
  padding-left: 15px;
  border-left: 5px solid #e74c3c;
}

.items-list {
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.menu-item {
  padding: 25px;
  background-color: #f8f9fa;
  border-radius: 12px;
  border: 2px solid #e9ecef;
  transition: all 0.3s ease;
}

.menu-item:hover {
  background-color: #fff;
  border-color: #e74c3c;
  transform: translateX(10px);
  box-shadow: 0 5px 15px rgba(231, 76, 60, 0.1);
}

.item-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.item-name {
  font-size: 24px;
  color: #2c3e50;
  margin: 0;
  font-weight: 600;
}

.item-price {
  font-size: 22px;
  color: #27ae60;
  font-weight: bold;
  background-color: #d5f4e6;
  padding: 5px 15px;
  border-radius: 20px;
}

.item-description {
  font-size: 16px;
  color: #7f8c8d;
  margin: 0;
  line-height: 1.6;
  font-style: italic;
}

/* Адаптивність */
@media (max-width: 768px) {
  .menu {
    padding: 20px;
  }

  .menu-title {
    font-size: 32px;
  }

  .category-name {
    font-size: 26px;
  }

  .item-header {
    flex-direction: column;
    align-items: flex-start;
    gap: 10px;
  }

  .item-name {
    font-size: 20px;
  }

  .item-price {
    font-size: 18px;
  }
}
```

---

## Використання в `App.jsx`

```jsx
// App.jsx
import "./App.css";
import UserProfile from "./components/UserProfile";
import RestaurantMenu from "./components/RestaurantMenu";

export default function App() {
  return (
    <div className="app">
      <section className="section">
        <UserProfile />
      </section>

      <section className="section">
        <RestaurantMenu />
      </section>
    </div>
  );
}
```

### `App.css`

```css
/* App.css */
.app {
  min-height: 100vh;
  background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
  padding: 40px 20px;
  font-family: "Arial", sans-serif;
}

.section {
  margin-bottom: 60px;
}

.section:last-child {
  margin-bottom: 0;
}

/* Глобальні стилі */
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  margin: 0;
  padding: 0;
}
```

---

## Структура проєкту

```
src/
├── components/
│   ├── UserProfile.jsx
│   ├── UserProfile.css
│   ├── RestaurantMenu.jsx
│   └── RestaurantMenu.css
├── App.jsx
├── App.css
└── main.jsx
```

---

## Пояснення використання Fragment ✅

### 1. **Короткий синтаксис `<>...</>`**

```jsx
<>
  <img src={user.avatar} alt={user.name} />
  <h1>{user.name}</h1>
</>
```

**Коли використовувати:**

- Коли не потрібен атрибут `key`
- Для групування елементів без додаткових DOM-вузлів

### 2. **Повний синтаксис `<Fragment>...</Fragment>`**

```jsx
<Fragment key={index}>
  <li className="skill-item">{skill}</li>
</Fragment>
```

**Коли використовувати:**

- Коли потрібен атрибут `key` (в `.map()`)
- Для явного імпорту: `import { Fragment } from 'react'`

### 3. **Навіщо використовувати Fragment?**

**❌ Без Fragment (зайві div):**

```jsx
{
  user.skills.map((skill, index) => (
    <div key={index}>
      {" "}
      {/* Непотрібний div! */}
      <li>{skill}</li>
    </div>
  ));
}
```

**✅ З Fragment (чистий DOM):**

```jsx
{
  user.skills.map((skill, index) => (
    <Fragment key={index}>
      <li>{skill}</li>
    </Fragment>
  ));
}
```

**Переваги:**

- Чистіший DOM (без зайвих елементів)
- Кращі стилі (CSS працює як очікується)
- Кращі селектори (легше працювати з DOM)

### 4. **Де НЕ потрібен Fragment?**

```jsx
// ❌ Непотрібно - вже є обгортка
<div className="profile-card">
  <>
    <h1>Title</h1>
  </>
</div>

// ✅ Правильно - використовуйте обгортку
<div className="profile-card">
  <h1>Title</h1>
</div>
```

---

## Порівняння підходів

### Варіант 1: Без Fragment (багато div'ів)

```jsx
{
  menuData.map((category) => (
    <div key={category.categoryId}>
      {" "}
      {/* Зайвий div */}
      <div className="category">
        <h2>{category.categoryName}</h2>
        {category.items.map((item) => (
          <div key={item.id}>
            {" "}
            {/* Ще один зайвий div */}
            <div className="menu-item">
              <h3>{item.name}</h3>
              <p>{item.description}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  ));
}
```

### Варіант 2: З Fragment (чистий код)

```jsx
{
  menuData.map((category) => (
    <Fragment key={category.categoryId}>
      <div className="category">
        <h2>{category.categoryName}</h2>
        {category.items.map((item) => (
          <Fragment key={item.id}>
            <div className="menu-item">
              <h3>{item.name}</h3>
              <p>{item.description}</p>
            </div>
          </Fragment>
        ))}
      </div>
    </Fragment>
  ));
}
```

---

## Запуск проєкту

```bash
npm run dev
```

Відкрийте браузер та насолоджуйтесь чистим DOM без зайвих елементів! 🎉

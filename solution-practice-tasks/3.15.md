# Рішення: Custom Hook useToggle

## Крок 1: Створіть файл `useToggle.js`

```js
// useToggle.js
import { useState, useCallback } from "react";

/**
 * Custom Hook для керування булевим станом
 * @param {boolean} initialValue - Початкове значення (за замовчуванням false)
 * @returns {[boolean, function, function, function]} - [value, toggle, setTrue, setFalse]
 */
const useToggle = (initialValue = false) => {
  // Стан для зберігання булевого значення
  const [value, setValue] = useState(initialValue);

  // Функція для інвертування значення
  const toggle = useCallback(() => {
    setValue((prev) => !prev);
  }, []);

  // Функція для встановлення значення на true
  const setTrue = useCallback(() => {
    setValue(true);
  }, []);

  // Функція для встановлення значення на false
  const setFalse = useCallback(() => {
    setValue(false);
  }, []);

  // Повертаємо масив з поточним значенням та функціями
  return [value, toggle, setTrue, setFalse];
};

export default useToggle;
```

---

## Крок 2: Створіть компонент `Modal.jsx`

```jsx
// Modal.jsx
import useToggle from "../hooks/useToggle";
import "./Modal.css";

export default function Modal() {
  // Використовуємо наш custom hook
  const [isOpen, toggle, open, close] = useToggle(false);

  return (
    <div className="modal-demo">
      <h2>🪟 Модальне вікно</h2>

      <div className="controls">
        <button className="btn btn-primary" onClick={open}>
          Відкрити модальне вікно
        </button>
        <button className="btn btn-secondary" onClick={toggle}>
          Перемкнути стан
        </button>
      </div>

      <p className="status">
        Статус: <strong>{isOpen ? "Відкрито ✅" : "Закрито ❌"}</strong>
      </p>

      {/* Модальне вікно */}
      {isOpen && (
        <div className="modal-overlay" onClick={close}>
          <div className="modal-content" onClick={(e) => e.stopPropagation()}>
            <div className="modal-header">
              <h3>Модальне вікно</h3>
              <button className="close-button" onClick={close}>
                ✕
              </button>
            </div>
            <div className="modal-body">
              <p>
                Це приклад модального вікна, керованого за допомогою useToggle!
              </p>
              <p>
                Натисніть на затемнену область або кнопку "Закрити" для
                закриття.
              </p>
            </div>
            <div className="modal-footer">
              <button className="btn btn-danger" onClick={close}>
                Закрити
              </button>
              <button className="btn btn-success" onClick={toggle}>
                Перемкнути
              </button>
            </div>
          </div>
        </div>
      )}
    </div>
  );
}
```

---

## Крок 3: Створіть компонент `Sidebar.jsx`

```jsx
// Sidebar.jsx
import useToggle from "../hooks/useToggle";
import "./Sidebar.css";

export default function Sidebar() {
  // Використовуємо той самий hook, але зі своїм ізольованим станом
  const [isVisible, toggle, show, hide] = useToggle(true);

  return (
    <div className="sidebar-demo">
      <h2>📂 Бічна панель</h2>

      <div className="controls">
        <button className="btn btn-primary" onClick={show}>
          Показати панель
        </button>
        <button className="btn btn-danger" onClick={hide}>
          Приховати панель
        </button>
        <button className="btn btn-secondary" onClick={toggle}>
          Перемкнути
        </button>
      </div>

      <p className="status">
        Статус: <strong>{isVisible ? "Видима ✅" : "Прихована ❌"}</strong>
      </p>

      <div className="sidebar-container">
        {/* Бічна панель */}
        <aside className={`sidebar ${isVisible ? "visible" : "hidden"}`}>
          <div className="sidebar-header">
            <h3>Навігація</h3>
            <button className="close-button" onClick={hide}>
              ✕
            </button>
          </div>
          <nav className="sidebar-nav">
            <a href="#home" className="nav-item">
              🏠 Головна
            </a>
            <a href="#about" className="nav-item">
              ℹ️ Про нас
            </a>
            <a href="#services" className="nav-item">
              ⚙️ Послуги
            </a>
            <a href="#contact" className="nav-item">
              📞 Контакти
            </a>
          </nav>
        </aside>

        {/* Основний контент */}
        <main className="main-content">
          <h3>Основний контент</h3>
          <p>Це основна область контенту.</p>
          <p>Бічна панель незалежно керується за допомогою useToggle.</p>
        </main>
      </div>
    </div>
  );
}
```

---

## Крок 4: Створіть компонент `Accordion.jsx`

```jsx
// Accordion.jsx
import useToggle from "../hooks/useToggle";
import "./Accordion.css";

function AccordionItem({ title, children }) {
  const [isOpen, toggle] = useToggle(false);

  return (
    <div className="accordion-item">
      <div className="accordion-header" onClick={toggle}>
        <h3>{title}</h3>
        <span className="accordion-icon">{isOpen ? "▲" : "▼"}</span>
      </div>
      {isOpen && <div className="accordion-content">{children}</div>}
    </div>
  );
}

export default function Accordion() {
  return (
    <div className="accordion-demo">
      <h2>📋 Акордеон</h2>

      <div className="accordion">
        <AccordionItem title="Що таке Custom Hook?">
          <p>
            Custom Hook — це JavaScript функція, яка використовує інші хуки
            React (useState, useEffect, тощо) для інкапсуляції логіки, яку можна
            повторно використовувати в різних компонентах.
          </p>
        </AccordionItem>

        <AccordionItem title="Навіщо потрібен useToggle?">
          <p>
            useToggle спрощує керування булевим станом, надаючи зручні функції
            для перемикання, встановлення true/false без дублювання коду.
          </p>
        </AccordionItem>

        <AccordionItem title="Як використовувати useToggle?">
          <p>Просто викличте useToggle() у вашому компоненті:</p>
          <code>
            const [isOpen, toggle, setTrue, setFalse] = useToggle(false);
          </code>
        </AccordionItem>
      </div>
    </div>
  );
}
```

---

## Крок 5: Створіть CSS файли

### `Modal.css`

```css
/* Modal.css */
.modal-demo {
  background-color: rgba(255, 255, 255, 0.15);
  border: 2px solid rgba(255, 255, 255, 0.3);
  border-radius: 16px;
  padding: 30px;
  margin-bottom: 30px;
}

.modal-demo h2 {
  margin-top: 0;
  margin-bottom: 20px;
  font-size: 28px;
  color: #fff;
}

.controls {
  display: flex;
  gap: 10px;
  margin-bottom: 20px;
  flex-wrap: wrap;
}

.btn {
  padding: 12px 24px;
  font-size: 16px;
  font-weight: bold;
  border: none;
  border-radius: 8px;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
}

.btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 15px rgba(0, 0, 0, 0.3);
}

.btn-primary {
  background-color: #3498db;
  color: #fff;
}

.btn-secondary {
  background-color: #95a5a6;
  color: #fff;
}

.btn-success {
  background-color: #27ae60;
  color: #fff;
}

.btn-danger {
  background-color: #e74c3c;
  color: #fff;
}

.status {
  font-size: 18px;
  color: rgba(255, 255, 255, 0.9);
  margin: 0;
}

.status strong {
  color: #ffd700;
}

/* Модальне вікно */
.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.7);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 1000;
  animation: fadeIn 0.3s ease;
}

@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

.modal-content {
  background-color: #fff;
  border-radius: 16px;
  max-width: 500px;
  width: 90%;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
  animation: slideDown 0.3s ease;
}

@keyframes slideDown {
  from {
    transform: translateY(-50px);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 30px;
  border-bottom: 2px solid #ecf0f1;
}

.modal-header h3 {
  margin: 0;
  color: #2c3e50;
  font-size: 24px;
}

.close-button {
  background: none;
  border: none;
  font-size: 28px;
  color: #7f8c8d;
  cursor: pointer;
  transition: all 0.3s ease;
}

.close-button:hover {
  color: #e74c3c;
  transform: rotate(90deg);
}

.modal-body {
  padding: 30px;
  color: #2c3e50;
  line-height: 1.6;
}

.modal-footer {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  padding: 20px 30px;
  border-top: 2px solid #ecf0f1;
}
```

### `Sidebar.css`

```css
/* Sidebar.css */
.sidebar-demo {
  background-color: rgba(255, 255, 255, 0.15);
  border: 2px solid rgba(255, 255, 255, 0.3);
  border-radius: 16px;
  padding: 30px;
  margin-bottom: 30px;
}

.sidebar-demo h2 {
  margin-top: 0;
  margin-bottom: 20px;
  font-size: 28px;
  color: #fff;
}

.sidebar-container {
  display: flex;
  gap: 0;
  background-color: #ecf0f1;
  border-radius: 12px;
  overflow: hidden;
  height: 400px;
  margin-top: 20px;
}

.sidebar {
  width: 250px;
  background-color: #2c3e50;
  color: #fff;
  transition: transform 0.3s ease;
  flex-shrink: 0;
}

.sidebar.hidden {
  transform: translateX(-100%);
}

.sidebar.visible {
  transform: translateX(0);
}

.sidebar-header {
  padding: 20px;
  border-bottom: 2px solid #34495e;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.sidebar-header h3 {
  margin: 0;
  font-size: 20px;
}

.sidebar-nav {
  padding: 20px;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.nav-item {
  display: block;
  padding: 12px 15px;
  color: #ecf0f1;
  text-decoration: none;
  border-radius: 8px;
  transition: all 0.3s ease;
}

.nav-item:hover {
  background-color: #34495e;
  transform: translateX(5px);
}

.main-content {
  flex: 1;
  padding: 30px;
  background-color: #fff;
  color: #2c3e50;
}

.main-content h3 {
  margin-top: 0;
  color: #2c3e50;
}
```

### `Accordion.css`

```css
/* Accordion.css */
.accordion-demo {
  background-color: rgba(255, 255, 255, 0.15);
  border: 2px solid rgba(255, 255, 255, 0.3);
  border-radius: 16px;
  padding: 30px;
}

.accordion-demo h2 {
  margin-top: 0;
  margin-bottom: 20px;
  font-size: 28px;
  color: #fff;
}

.accordion {
  display: flex;
  flex-direction: column;
  gap: 15px;
}

.accordion-item {
  background-color: #fff;
  border-radius: 12px;
  overflow: hidden;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
}

.accordion-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 25px;
  background-color: #3498db;
  color: #fff;
  cursor: pointer;
  transition: all 0.3s ease;
}

.accordion-header:hover {
  background-color: #2980b9;
}

.accordion-header h3 {
  margin: 0;
  font-size: 20px;
}

.accordion-icon {
  font-size: 18px;
  transition: transform 0.3s ease;
}

.accordion-content {
  padding: 25px;
  color: #2c3e50;
  line-height: 1.6;
  animation: slideDown 0.3s ease;
}

@keyframes slideDown {
  from {
    opacity: 0;
    transform: translateY(-10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.accordion-content code {
  display: block;
  background-color: #ecf0f1;
  padding: 12px;
  border-radius: 6px;
  font-family: "Courier New", monospace;
  margin-top: 10px;
  color: #e74c3c;
}
```

---

## Крок 6: Використання в `App.jsx`

```jsx
// App.jsx
import "./App.css";
import Modal from "./components/Modal";
import Sidebar from "./components/Sidebar";
import Accordion from "./components/Accordion";

export default function App() {
  return (
    <div className="app">
      <h1 className="main-title">🎨 Custom Hook: useToggle</h1>

      <Modal />
      <Sidebar />
      <Accordion />
    </div>
  );
}
```

### `App.css`

```css
/* App.css */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  margin: 0;
  padding: 0;
  font-family: "Arial", sans-serif;
}

.app {
  min-height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  padding: 40px 20px;
}

.main-title {
  text-align: center;
  font-size: 42px;
  color: #fff;
  margin-bottom: 40px;
  font-weight: bold;
  text-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
}

@media (max-width: 768px) {
  .main-title {
    font-size: 32px;
  }
}
```

---

## Структура проєкту

```
src/
├── hooks/
│   └── useToggle.js
├── components/
│   ├── Modal.jsx
│   ├── Modal.css
│   ├── Sidebar.jsx
│   ├── Sidebar.css
│   ├── Accordion.jsx
│   └── Accordion.css
├── App.jsx
├── App.css
└── main.jsx
```

---

## Пояснення Custom Hooks ✅

### **Що таке Custom Hook?**

Custom Hook — це JavaScript функція, яка:

1. Починається з префіксу `use` (конвенція React)
2. Використовує вбудовані хуки React (useState, useEffect, тощо)
3. Інкапсулює логіку для повторного використання

### **Переваги Custom Hooks:**

✅ **Повторне використання логіки** — пишете один раз, використовуєте скрізь
✅ **Чистіший код** — компоненти стають простішими
✅ **Легше тестувати** — логіка відокремлена від UI
✅ **Ізоляція стану** — кожен виклик хука має власний стан

---

## Як працює useToggle

### **Внутрішня структура:**

```js
const useToggle = (initialValue = false) => {
  const [value, setValue] = useState(initialValue);

  const toggle = useCallback(() => setValue((prev) => !prev), []);
  const setTrue = useCallback(() => setValue(true), []);
  const setFalse = useCallback(() => setValue(false), []);

  return [value, toggle, setTrue, setFalse];
};
```

### **Чому useCallback?**

`useCallback` мемоїзує функції, щоб вони не створювалися знову при кожному рендері. Це оптимізація для продуктивності.

### **Використання:**

```jsx
const [isOpen, toggle, open, close] = useToggle(false);

// isOpen — поточне значення (false)
// toggle() — інвертує значення
// open() — встановлює true
// close() — встановлює false
```

---

## Порівняння: З useToggle vs Без useToggle

### ❌ Без useToggle (дублювання коду)

```jsx
function Modal() {
  const [isOpen, setIsOpen] = useState(false);
  const toggle = () => setIsOpen(!isOpen);
  const open = () => setIsOpen(true);
  const close = () => setIsOpen(false);
  // ...
}

function Sidebar() {
  const [isVisible, setIsVisible] = useState(true);
  const toggle = () => setIsVisible(!isVisible);
  const show = () => setIsVisible(true);
  const hide = () => setIsVisible(false);
  // ...
}
```

**Проблеми:**

- Дублювання логіки в кожному компоненті
- Більше коду для підтримки
- Легко зробити помилку

### ✅ З useToggle (чистий код)

```jsx
function Modal() {
  const [isOpen, toggle, open, close] = useToggle(false);
  // ...
}

function Sidebar() {
  const [isVisible, toggle, show, hide] = useToggle(true);
  // ...
}
```

**Переваги:**

- Один рядок замість чотирьох
- Логіка централізована
- Легко підтримувати

---

## Інші корисні Custom Hooks

### useLocalStorage

```js
function useLocalStorage(key, initialValue) {
  const [value, setValue] = useState(() => {
    const item = localStorage.getItem(key);
    return item ? JSON.parse(item) : initialValue;
  });

  const setStoredValue = (newValue) => {
    setValue(newValue);
    localStorage.setItem(key, JSON.stringify(newValue));
  };

  return [value, setStoredValue];
}
```

### useWindowSize

```js
function useWindowSize() {
  const [size, setSize] = useState({
    width: window.innerWidth,
    height: window.innerHeight,
  });

  useEffect(() => {
    const handleResize = () => {
      setSize({
        width: window.innerWidth,
        height: window.innerHeight,
      });
    };

    window.addEventListener("resize", handleResize);
    return () => window.removeEventListener("resize", handleResize);
  }, []);

  return size;
}
```

---

## Запуск проєкту

```bash
npm run dev
```

**Тестування:**

1. Відкрийте модальне вікно
2. Перемикайте бічну панель
3. Розгортайте елементи акордеону
4. Зверніть увагу, що всі компоненти використовують один хук, але мають ізольований стан

🎉 Готово!

# Рішення: Умовний рендеринг у React

## Завдання 1: Компонент "Статус користувача"

### Створіть файл `UserStatus.jsx`

```jsx
// UserStatus.jsx
export default function UserStatus({ isLoggedIn, username, role, isOnline }) {
  // 1. Early return для неавторизованих користувачів (if/else)
  if (!isLoggedIn) {
    return (
      <div style={styles.container}>
        <p>Будь ласка, увійдіть в акаунт</p>
      </div>
    );
  }

  // 2. Тернарний оператор для привітання залежно від ролі
  const greeting =
    role === "admin"
      ? `Вітаємо, Адміністратор ${username}`
      : role === "user"
      ? `Привіт, ${username}`
      : "Привіт, Гість";

  // 3. Тернарний оператор для статусу онлайн
  const onlineStatus = isOnline ? "🟢 В мережі" : "⚫ Офлайн";

  return (
    <div style={styles.container}>
      <h2>{greeting}</h2>
      <p style={styles.status}>{onlineStatus}</p>

      {/* 4. Логічний && для кнопки адмін-панелі */}
      {role === "admin" && (
        <button style={styles.adminButton}>Панель адміністратора</button>
      )}

      {/* 5. Логічний && для VIP бейджа (онлайн ТА admin) */}
      {isOnline && role === "admin" && (
        <span style={styles.vipBadge}>⭐ VIP</span>
      )}
    </div>
  );
}

// Стилі для компонента
const styles = {
  container: {
    border: "2px solid #ddd",
    borderRadius: "8px",
    padding: "20px",
    margin: "20px 0",
    backgroundColor: "#f9f9f9",
  },
  status: {
    fontSize: "16px",
    fontWeight: "bold",
    margin: "10px 0",
  },
  adminButton: {
    backgroundColor: "#dc3545",
    color: "white",
    border: "none",
    padding: "10px 20px",
    borderRadius: "5px",
    cursor: "pointer",
    fontSize: "14px",
    marginTop: "10px",
  },
  vipBadge: {
    backgroundColor: "#ffc107",
    color: "#000",
    padding: "5px 15px",
    borderRadius: "20px",
    fontSize: "14px",
    fontWeight: "bold",
    marginLeft: "10px",
    display: "inline-block",
    marginTop: "10px",
  },
};
```

---

## Завдання 2: Компонент "Картка товару"

### Створіть файл `ProductCard.jsx`

```jsx
// ProductCard.jsx
export default function ProductCard({
  name,
  price,
  inStock,
  discount,
  rating,
  isFeatured,
  freeShipping,
}) {
  // 1. Early return для товарів не в наявності (if/else)
  if (!inStock) {
    return (
      <div style={styles.cardUnavailable}>
        <h3>{name}</h3>
        <p style={styles.unavailableText}>❌ Товар недоступний</p>
        <button style={styles.notifyButton}>Повідомити про надходження</button>
      </div>
    );
  }

  // Розрахунок ціни зі знижкою
  const discountedPrice =
    discount > 0 ? price - (price * discount) / 100 : price;

  // Генерація зірочок для рейтингу
  const stars = "⭐".repeat(rating);

  // 2. Тернарний оператор для тексту кнопки
  const buttonText = inStock ? "Купити" : "Повідомити про надходження";

  return (
    <div style={styles.card}>
      {/* 3. Логічний && для бейджа "Рекомендуємо" */}
      {isFeatured && <span style={styles.featuredBadge}>🔥 Рекомендуємо</span>}

      {/* 4. Логічний && для бейджа знижки */}
      {discount > 0 && <span style={styles.discountBadge}>-{discount}%</span>}

      <h3>{name}</h3>

      {/* 5. Тернарний оператор для відображення ціни */}
      <div style={styles.priceContainer}>
        {discount > 0 ? (
          <>
            <span style={styles.oldPrice}>{price} грн</span>
            <span style={styles.newPrice}>
              {discountedPrice.toFixed(0)} грн
            </span>
          </>
        ) : (
          <span style={styles.price}>{price} грн</span>
        )}
      </div>

      {/* 6. Логічний && для рейтингу */}
      {rating > 0 && (
        <p style={styles.rating}>
          {stars} ({rating}/5)
        </p>
      )}

      {/* 7. Логічний && для безкоштовної доставки */}
      {freeShipping && <p style={styles.shipping}>🚚 Безкоштовна доставка</p>}

      {/* 8. Тернарний оператор для статусу наявності */}
      <p style={styles.stockStatus}>
        {inStock ? "✅ В наявності" : "❌ Немає в наявності"}
      </p>

      <button style={styles.buyButton}>{buttonText}</button>
    </div>
  );
}

// Стилі для компонента
const styles = {
  card: {
    border: "2px solid #28a745",
    borderRadius: "12px",
    padding: "20px",
    margin: "20px",
    backgroundColor: "#fff",
    position: "relative",
    boxShadow: "0 2px 8px rgba(0,0,0,0.1)",
    maxWidth: "300px",
  },
  cardUnavailable: {
    border: "2px solid #ccc",
    borderRadius: "12px",
    padding: "20px",
    margin: "20px",
    backgroundColor: "#f0f0f0",
    position: "relative",
    boxShadow: "0 2px 8px rgba(0,0,0,0.1)",
    maxWidth: "300px",
  },
  featuredBadge: {
    position: "absolute",
    top: "10px",
    left: "10px",
    backgroundColor: "#ff6b6b",
    color: "white",
    padding: "5px 10px",
    borderRadius: "5px",
    fontSize: "12px",
    fontWeight: "bold",
  },
  discountBadge: {
    position: "absolute",
    top: "10px",
    right: "10px",
    backgroundColor: "#ffc107",
    color: "#000",
    padding: "5px 10px",
    borderRadius: "5px",
    fontSize: "14px",
    fontWeight: "bold",
  },
  priceContainer: {
    margin: "15px 0",
  },
  price: {
    fontSize: "24px",
    fontWeight: "bold",
    color: "#28a745",
  },
  oldPrice: {
    fontSize: "18px",
    textDecoration: "line-through",
    color: "#999",
    marginRight: "10px",
  },
  newPrice: {
    fontSize: "24px",
    fontWeight: "bold",
    color: "#dc3545",
  },
  rating: {
    fontSize: "16px",
    margin: "10px 0",
  },
  shipping: {
    color: "#28a745",
    fontWeight: "bold",
    fontSize: "14px",
  },
  stockStatus: {
    fontSize: "14px",
    fontWeight: "bold",
    margin: "10px 0",
  },
  buyButton: {
    backgroundColor: "#007bff",
    color: "white",
    border: "none",
    padding: "12px 24px",
    borderRadius: "6px",
    cursor: "pointer",
    fontSize: "16px",
    width: "100%",
    fontWeight: "bold",
    marginTop: "10px",
  },
  notifyButton: {
    backgroundColor: "#6c757d",
    color: "white",
    border: "none",
    padding: "12px 24px",
    borderRadius: "6px",
    cursor: "pointer",
    fontSize: "16px",
    width: "100%",
    fontWeight: "bold",
    marginTop: "10px",
  },
  unavailableText: {
    fontSize: "18px",
    fontWeight: "bold",
    color: "#dc3545",
    margin: "15px 0",
  },
};
```

---

## Використання компонентів у `App.jsx`

```jsx
// App.jsx
import UserStatus from "./components/UserStatus";
import ProductCard from "./components/ProductCard";

export default function App() {
  return (
    <div style={{ padding: "40px", maxWidth: "1200px", margin: "0 auto" }}>
      <h1>Демонстрація умовного рендерингу</h1>

      <section>
        <h2>Завдання 1: Статус користувача</h2>

        <h3>Неавторизований користувач</h3>
        <UserStatus
          isLoggedIn={false}
          username="Олена"
          role="user"
          isOnline={true}
        />

        <h3>Адміністратор онлайн</h3>
        <UserStatus
          isLoggedIn={true}
          username="Олена"
          role="admin"
          isOnline={true}
        />

        <h3>Звичайний користувач офлайн</h3>
        <UserStatus
          isLoggedIn={true}
          username="Іван"
          role="user"
          isOnline={false}
        />

        <h3>Гість онлайн</h3>
        <UserStatus
          isLoggedIn={true}
          username=""
          role="guest"
          isOnline={true}
        />
      </section>

      <section style={{ marginTop: "40px" }}>
        <h2>Завдання 2: Картка товару</h2>

        <div style={{ display: "flex", flexWrap: "wrap", gap: "20px" }}>
          <div>
            <h3>Товар недоступний</h3>
            <ProductCard
              name="Ноутбук Lenovo"
              price={25000}
              inStock={false}
              discount={0}
              rating={4}
              isFeatured={true}
              freeShipping={false}
            />
          </div>

          <div>
            <h3>Товар зі знижкою та всіма бейджами</h3>
            <ProductCard
              name="Навушники Sony"
              price={3500}
              inStock={true}
              discount={15}
              rating={5}
              isFeatured={true}
              freeShipping={true}
            />
          </div>

          <div>
            <h3>Звичайний товар без знижки</h3>
            <ProductCard
              name="Клавіатура Logitech"
              price={1200}
              inStock={true}
              discount={0}
              rating={4}
              isFeatured={false}
              freeShipping={false}
            />
          </div>

          <div>
            <h3>Товар з великою знижкою</h3>
            <ProductCard
              name="Миша Razer"
              price={2000}
              inStock={true}
              discount={30}
              rating={5}
              isFeatured={true}
              freeShipping={true}
            />
          </div>
        </div>
      </section>
    </div>
  );
}
```

---

## Структура проєкту

```
src/
├── components/
│   ├── UserStatus.jsx
│   └── ProductCard.jsx
├── App.jsx
└── main.jsx
```

---

## Основні техніки умовного рендерингу ✅

### 1. **Early Return (if/else)**

```jsx
if (!isLoggedIn) {
  return <div>Будь ласка, увійдіть в акаунт</div>;
}
```

### 2. **Тернарний оператор**

```jsx
{
  isOnline ? "🟢 В мережі" : "⚫ Офлайн";
}
```

### 3. **Логічний оператор &&**

```jsx
{
  role === "admin" && <button>Панель адміністратора</button>;
}
```

### 4. **Вкладені тернарні оператори**

```jsx
{
  role === "admin" ? "Адміністратор" : role === "user" ? "Користувач" : "Гість";
}
```

---

## Запуск проєкту

```bash
npm run dev
```

Відкрийте браузер та перевірте різні варіанти відображення компонентів! 🎉

# Рішення: useOptimistic для оптимістичних оновлень

## ⚠️ Важливе зауваження

**useOptimistic** - це експериментальний хук з React 19 canary. Якщо ви використовуєте React 18, потрібно оновитися до React 19 або використати альтернативний підхід.

### Варіанти реалізації:

1. **React 19** - використовуємо `useOptimistic`
2. **React 18** - використовуємо альтернативний підхід з `useState`

Нижче надано обидва варіанти рішення.

---

## Варіант 1: React 19 з useOptimistic

### Крок 1: Створення CommentFeed компонента (React 19)

Створіть файл `CommentFeed.jsx` у папці `src/components`:

```jsx
import React, { useState, useTransition, useOptimistic } from "react";

const CommentFeed = () => {
  // Реальний стан коментарів (підтверджені сервером)
  const [comments, setComments] = useState([
    {
      id: 1,
      text: "Чудовий додаток!",
      author: "User-A",
      timestamp: Date.now(),
    },
    {
      id: 2,
      text: "Дуже зручний інтерфейс",
      author: "User-B",
      timestamp: Date.now(),
    },
    {
      id: 3,
      text: "Працює швидко та стабільно",
      author: "User-C",
      timestamp: Date.now(),
    },
  ]);

  // Оптимістичний стан коментарів
  const [optimisticComments, addOptimisticComment] = useOptimistic(
    comments,
    (currentComments, newComment) => {
      // Додаємо новий коментар з прапором isPending
      return [...currentComments, { ...newComment, isPending: true }];
    }
  );

  // Transition для асинхронних операцій
  const [isPending, startTransition] = useTransition();

  // Стан для форми
  const [commentText, setCommentText] = useState("");

  // Обробник відправки коментаря
  const handleSendComment = async (text) => {
    if (!text.trim()) return;

    // Генеруємо новий коментар
    const newComment = {
      id: Date.now(),
      text: text.trim(),
      author: "Поточний користувач",
      timestamp: Date.now(),
    };

    // 1. ОПТИМІСТИЧНЕ ОНОВЛЕННЯ - миттєво додаємо в UI
    addOptimisticComment(newComment);

    // Очищаємо форму
    setCommentText("");

    // 2. АСИНХРОННА ДІЯ - відправка на сервер
    startTransition(async () => {
      console.log("📤 Відправка коментаря:", newComment);

      // Імітація API запиту (затримка 1.5 секунди)
      await new Promise((resolve) => setTimeout(resolve, 1500));

      // Імітація помилки якщо текст містить "bug"
      if (text.toLowerCase().includes("bug")) {
        console.error("❌ Помилка: коментар містить заборонене слово");
        // НЕ оновлюємо реальний стан - відбудеться автоматичне відкочування
        alert('Помилка: коментар не може містити слово "bug"');
        return;
      }

      // 3. УСПІХ - оновлюємо реальний стан
      console.log("✅ Коментар успішно додано");
      setComments((prev) => [...prev, newComment]);
    });
  };

  // Обробник форми
  const handleSubmit = (e) => {
    e.preventDefault();
    handleSendComment(commentText);
  };

  return (
    <div className="comment-feed-container">
      <div className="comment-feed-card">
        <h2>💬 Стрічка Коментарів</h2>
        <p className="feed-subtitle">
          React 19 useOptimistic - оптимістичні оновлення
        </p>

        {/* Форма додавання коментаря */}
        <form onSubmit={handleSubmit} className="comment-form">
          <div className="form-group">
            <textarea
              value={commentText}
              onChange={(e) => setCommentText(e.target.value)}
              placeholder="Напишіть ваш коментар..."
              rows="3"
              className="comment-textarea"
              disabled={isPending}
            />
            <span className="form-hint">
              💡 Спробуйте написати "bug" для імітації помилки
            </span>
          </div>
          <button
            type="submit"
            className="send-comment-button"
            disabled={!commentText.trim() || isPending}
          >
            {isPending ? "📤 Надсилання..." : "📤 Відправити коментар"}
          </button>
        </form>

        {/* Список коментарів */}
        <div className="comments-list">
          <h3>Коментарі ({optimisticComments.length})</h3>

          {optimisticComments.map((comment) => (
            <div
              key={comment.id}
              className={`comment-item ${
                comment.isPending ? "comment-pending" : ""
              }`}
            >
              <div className="comment-header">
                <span className="comment-author">👤 {comment.author}</span>
                <span className="comment-time">
                  {new Date(comment.timestamp).toLocaleTimeString("uk-UA")}
                </span>
              </div>
              <p className="comment-text">{comment.text}</p>

              {comment.isPending && (
                <div className="pending-indicator">
                  <span className="spinner-tiny"></span>
                  Надсилання...
                </div>
              )}
            </div>
          ))}
        </div>

        {/* Інформаційний блок */}
        <div className="optimistic-info">
          <h4>ℹ️ Як працює useOptimistic:</h4>
          <div className="info-steps">
            <div className="info-step">
              <span className="step-number">1</span>
              <div className="step-content">
                <strong>Оптимістичне оновлення</strong>
                <p>Коментар миттєво з'являється в UI</p>
              </div>
            </div>
            <div className="info-step">
              <span className="step-number">2</span>
              <div className="step-content">
                <strong>Відправка на сервер</strong>
                <p>Асинхронний запит в startTransition</p>
              </div>
            </div>
            <div className="info-step">
              <span className="step-number">3</span>
              <div className="step-content">
                <strong>Підтвердження або відкочування</strong>
                <p>
                  Успіх → оновлюємо реальний стан
                  <br />
                  Помилка → автоматичне відкочування
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default CommentFeed;
```

---

## Варіант 2: React 18 - Альтернативна реалізація

### Крок 2: Створення CommentFeedLegacy (React 18)

Створіть файл `CommentFeedLegacy.jsx` у папці `src/components`:

```jsx
import React, { useState, useTransition } from "react";

const CommentFeedLegacy = () => {
  // Підтверджені коментарі
  const [comments, setComments] = useState([
    {
      id: 1,
      text: "Чудовий додаток!",
      author: "User-A",
      timestamp: Date.now(),
    },
    {
      id: 2,
      text: "Дуже зручний інтерфейс",
      author: "User-B",
      timestamp: Date.now(),
    },
    {
      id: 3,
      text: "Працює швидко та стабільно",
      author: "User-C",
      timestamp: Date.now(),
    },
  ]);

  // Оптимістичні коментарі (що очікують підтвердження)
  const [pendingComments, setPendingComments] = useState([]);

  const [isPending, startTransition] = useTransition();
  const [commentText, setCommentText] = useState("");

  // Комбінований список (підтверджені + очікуючі)
  const allComments = [...comments, ...pendingComments];

  const handleSendComment = async (text) => {
    if (!text.trim()) return;

    const newComment = {
      id: Date.now(),
      text: text.trim(),
      author: "Поточний користувач",
      timestamp: Date.now(),
      isPending: true,
    };

    // 1. Додаємо в оптимістичний стан
    setPendingComments((prev) => [...prev, newComment]);
    setCommentText("");

    // 2. Асинхронна відправка
    startTransition(async () => {
      console.log("📤 Відправка коментаря:", newComment);

      await new Promise((resolve) => setTimeout(resolve, 1500));

      // Імітація помилки
      if (text.toLowerCase().includes("bug")) {
        console.error("❌ Помилка сервера");
        // Видаляємо з pending
        setPendingComments((prev) =>
          prev.filter((c) => c.id !== newComment.id)
        );
        alert('Помилка: коментар не може містити слово "bug"');
        return;
      }

      // Успіх - переміщуємо з pending в підтверджені
      console.log("✅ Коментар підтверджено");
      setPendingComments((prev) => prev.filter((c) => c.id !== newComment.id));
      setComments((prev) => [...prev, { ...newComment, isPending: false }]);
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    handleSendComment(commentText);
  };

  return (
    <div className="comment-feed-container">
      <div className="comment-feed-card">
        <h2>💬 Стрічка Коментарів</h2>
        <p className="feed-subtitle">
          React 18 - Альтернативна реалізація з useState
        </p>

        <form onSubmit={handleSubmit} className="comment-form">
          <div className="form-group">
            <textarea
              value={commentText}
              onChange={(e) => setCommentText(e.target.value)}
              placeholder="Напишіть ваш коментар..."
              rows="3"
              className="comment-textarea"
              disabled={isPending}
            />
            <span className="form-hint">
              💡 Спробуйте написати "bug" для імітації помилки
            </span>
          </div>
          <button
            type="submit"
            className="send-comment-button"
            disabled={!commentText.trim() || isPending}
          >
            {isPending ? "📤 Надсилання..." : "📤 Відправити коментар"}
          </button>
        </form>

        <div className="comments-list">
          <h3>Коментарі ({allComments.length})</h3>

          {allComments.map((comment) => (
            <div
              key={comment.id}
              className={`comment-item ${
                comment.isPending ? "comment-pending" : ""
              }`}
            >
              <div className="comment-header">
                <span className="comment-author">👤 {comment.author}</span>
                <span className="comment-time">
                  {new Date(comment.timestamp).toLocaleTimeString("uk-UA")}
                </span>
              </div>
              <p className="comment-text">{comment.text}</p>

              {comment.isPending && (
                <div className="pending-indicator">
                  <span className="spinner-tiny"></span>
                  Надсилання...
                </div>
              )}
            </div>
          ))}
        </div>

        <div className="optimistic-info">
          <h4>ℹ️ React 18 підхід:</h4>
          <ul>
            <li>Використовується окремий стан для pending коментарів</li>
            <li>Комбінуємо підтверджені + очікуючі коментарі</li>
            <li>Ручне керування відкочуванням при помилці</li>
          </ul>
        </div>
      </div>
    </div>
  );
};

export default CommentFeedLegacy;
```

---

## Крок 3: Додавання стилів

Додайте до `App.css`:

```css
/* Контейнер стрічки коментарів */
.comment-feed-container {
  padding: 40px 20px;
  max-width: 800px;
  margin: 0 auto;
}

.comment-feed-card {
  background: white;
  border-radius: 20px;
  padding: 40px;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
}

.feed-subtitle {
  color: #666;
  text-align: center;
  margin-bottom: 30px;
  font-size: 15px;
}

/* Форма коментаря */
.comment-form {
  margin-bottom: 40px;
  padding: 25px;
  background: #f9f9f9;
  border-radius: 15px;
}

.comment-textarea {
  width: 100%;
  padding: 15px;
  border: 2px solid #e0e0e0;
  border-radius: 10px;
  font-size: 15px;
  font-family: inherit;
  resize: vertical;
  transition: all 0.3s ease;
}

.comment-textarea:focus {
  outline: none;
  border-color: #667eea;
  box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
}

.comment-textarea:disabled {
  background: #f5f5f5;
  cursor: not-allowed;
}

.form-hint {
  display: block;
  margin-top: 10px;
  font-size: 13px;
  color: #999;
  font-style: italic;
}

.send-comment-button {
  width: 100%;
  margin-top: 15px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border: none;
  padding: 14px;
  border-radius: 10px;
  font-size: 16px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
}

.send-comment-button:hover:not(:disabled) {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(102, 126, 234, 0.4);
}

.send-comment-button:disabled {
  opacity: 0.6;
  cursor: not-allowed;
  transform: none;
}

/* Список коментарів */
.comments-list {
  margin-bottom: 30px;
}

.comments-list h3 {
  color: #333;
  margin-bottom: 20px;
  font-size: 20px;
  padding-bottom: 15px;
  border-bottom: 2px solid #f0f0f0;
}

.comment-item {
  background: white;
  border: 2px solid #e0e0e0;
  border-radius: 12px;
  padding: 20px;
  margin-bottom: 15px;
  transition: all 0.3s ease;
}

.comment-item:hover {
  border-color: #667eea;
  box-shadow: 0 4px 12px rgba(102, 126, 234, 0.1);
}

/* Оптимістичний коментар (pending) */
.comment-item.comment-pending {
  opacity: 0.6;
  background: linear-gradient(135deg, #667eea05 0%, #764ba205 100%);
  border-style: dashed;
  animation: pendingPulse 2s ease-in-out infinite;
}

@keyframes pendingPulse {
  0%,
  100% {
    opacity: 0.6;
  }
  50% {
    opacity: 0.8;
  }
}

.comment-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.comment-author {
  font-weight: 600;
  color: #667eea;
  font-size: 14px;
}

.comment-time {
  font-size: 12px;
  color: #999;
}

.comment-text {
  color: #333;
  line-height: 1.6;
  margin: 0;
  font-size: 15px;
}

/* Індикатор pending */
.pending-indicator {
  margin-top: 12px;
  padding-top: 12px;
  border-top: 1px solid #e0e0e0;
  display: flex;
  align-items: center;
  gap: 8px;
  color: #ff9800;
  font-size: 13px;
  font-weight: 600;
}

/* Інформаційний блок */
.optimistic-info {
  padding: 25px;
  background: linear-gradient(135deg, #667eea10 0%, #764ba210 100%);
  border-radius: 15px;
  border: 2px solid #667eea30;
}

.optimistic-info h4 {
  color: #333;
  margin-bottom: 20px;
  font-size: 18px;
}

.optimistic-info ul {
  list-style: none;
  padding: 0;
  margin: 0;
}

.optimistic-info li {
  color: #666;
  font-size: 14px;
  padding: 8px 0;
  padding-left: 25px;
  position: relative;
}

.optimistic-info li::before {
  content: "✓";
  position: absolute;
  left: 0;
  color: #667eea;
  font-weight: bold;
  font-size: 16px;
}

/* Кроки інформації */
.info-steps {
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.info-step {
  display: flex;
  align-items: start;
  gap: 15px;
  background: white;
  padding: 20px;
  border-radius: 10px;
  border: 2px solid #e0e0e0;
}

.step-number {
  width: 36px;
  height: 36px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: bold;
  flex-shrink: 0;
  font-size: 18px;
}

.step-content strong {
  display: block;
  color: #333;
  margin-bottom: 5px;
  font-size: 15px;
}

.step-content p {
  color: #666;
  font-size: 14px;
  margin: 0;
  line-height: 1.5;
}

/* Адаптивність */
@media (max-width: 768px) {
  .comment-feed-card {
    padding: 25px;
  }

  .comment-form {
    padding: 20px;
  }

  .info-steps {
    gap: 15px;
  }

  .info-step {
    padding: 15px;
  }
}
```

---

## Крок 4: Інтеграція в App.jsx

```jsx
// Виберіть потрібний варіант
import CommentFeed from './components/CommentFeed'; // React 19
// або
import CommentFeedLegacy from './components/CommentFeedLegacy'; // React 18

// В Routes:
<Route path="/comments" element={<CommentFeed />} />
// або
<Route path="/comments" element={<CommentFeedLegacy />} />
```

---

## Крок 5: Оновлення Navigation.jsx

```jsx
<li>
  <NavLink
    to="/comments"
    className={({ isActive }) => (isActive ? "active-link" : "default-link")}
  >
    💬 Коментарі
  </NavLink>
</li>
```

---

## Крок 6: Тестування

```bash
npm run dev
```

### Сценарії тестування:

#### Тест 1: Успішне додавання коментаря

1. Перейдіть на `/comments`
2. Введіть: "Чудовий функціонал!"
3. Натисніть "Відправити"
4. **Очікується:**
   - Коментар миттєво з'являється з напівпрозорістю
   - Показується "Надсилання..."
   - Через 1.5 сек стає повністю видимим

#### Тест 2: Відкочування при помилці

1. Введіть: "Тут є bug"
2. Натисніть "Відправити"
3. **Очікується:**
   - Коментар з'являється оптимістично
   - Через 1.5 сек зникає
   - Alert про помилку

#### Тест 3: Кілька коментарів підряд

1. Швидко додайте 3 коментарі
2. **Очікується:**
   - Всі з'являються миттєво
   - По черзі підтверджуються
   - UI залишається чутливим

---

## Результат

✅ **useOptimistic Logic** - Хук коректно додає `isPending: true`  
✅ **Розмежування** - `addOptimistic` миттєво, `setComments` в `startTransition`  
✅ **Відкочування** - Автоматичне при помилці  
✅ **UX** - Візуальна індикація працює коректно

---

## Висновок

**useOptimistic** забезпечує:

- ⚡ Миттєвий відгук UI
- 🔄 Автоматичне відкочування
- 📊 Краща користувацька взаємодія
- 🎯 Оптимістичні оновлення

```
Готово! Створив повне рішення для useOptimistic з двома варіантами (React 19 і React 18 альтернатива). Включено форму коментарів, оптимістичні оновлення, відкочування при помилках та повну стилізацію! 💬
```

# Рішення: Компонент "Рейтинг"

## Створіть файл `HighScores.jsx`

```jsx
// HighScores.jsx
import { useState } from "react";
import "./HighScores.css";

export default function HighScores() {
  // Початковий стан з кількома балами
  const [scores, setScores] = useState([85, 92, 78, 95, 88]);

  // Функція: Додати випадковий бал (0-100)
  const addRandomScore = () => {
    const randomScore = Math.floor(Math.random() * 101); // 0 до 100
    setScores([...scores, randomScore]); // Додаємо новий бал через spread
  };

  // Функція: Видалити мінімальний бал
  const removeMinScore = () => {
    if (scores.length === 0) {
      alert("Немає балів для видалення!");
      return;
    }

    const minScore = Math.min(...scores); // Знаходимо мінімальний бал
    const minIndex = scores.indexOf(minScore); // Знаходимо індекс мінімального балу

    // Видаляємо перший мінімальний бал через filter
    const newScores = scores.filter((score, index) => index !== minIndex);
    setScores(newScores);
  };

  // Функція: Збільшити всі бали на 10
  const increaseAllScores = () => {
    if (scores.length === 0) {
      alert("Немає балів для збільшення!");
      return;
    }

    // Збільшуємо кожен бал на 10 через map
    const increasedScores = scores.map((score) => score + 10);
    setScores(increasedScores);
  };

  // Обчислюємо статистику
  const getStatistics = () => {
    if (scores.length === 0) {
      return { min: 0, max: 0, avg: 0 };
    }

    const min = Math.min(...scores);
    const max = Math.max(...scores);
    const avg = (
      scores.reduce((sum, score) => sum + score, 0) / scores.length
    ).toFixed(1);

    return { min, max, avg };
  };

  const stats = getStatistics();

  return (
    <div className="high-scores">
      <h1 className="title">🏆 Таблиця рекордів</h1>

      {/* Статистика */}
      <div className="statistics">
        <div className="stat-card">
          <span className="stat-label">Всього балів:</span>
          <span className="stat-value">{scores.length}</span>
        </div>
        <div className="stat-card">
          <span className="stat-label">Мінімум:</span>
          <span className="stat-value">{stats.min}</span>
        </div>
        <div className="stat-card">
          <span className="stat-label">Максимум:</span>
          <span className="stat-value">{stats.max}</span>
        </div>
        <div className="stat-card">
          <span className="stat-label">Середнє:</span>
          <span className="stat-value">{stats.avg}</span>
        </div>
      </div>

      {/* Кнопки управління */}
      <div className="controls">
        <button className="btn btn-add" onClick={addRandomScore}>
          ➕ Додати випадковий бал
        </button>
        <button
          className="btn btn-remove"
          onClick={removeMinScore}
          disabled={scores.length === 0}
        >
          ➖ Видалити мінімальний бал
        </button>
        <button
          className="btn btn-increase"
          onClick={increaseAllScores}
          disabled={scores.length === 0}
        >
          ⬆️ Збільшити всі бали на 10
        </button>
      </div>

      {/* Список балів */}
      <div className="scores-container">
        {scores.length === 0 ? (
          <p className="empty-message">Немає жодного балу. Додайте перший!</p>
        ) : (
          <ul className="scores-list">
            {scores.map((score, index) => (
              <li
                key={index}
                className={`score-item ${
                  score === stats.min ? "min-score" : ""
                } ${score === stats.max ? "max-score" : ""}`}
              >
                <span className="score-number">#{index + 1}</span>
                <span className="score-value">{score}</span>
                <span
                  className="score-bar"
                  style={{ width: `${score}%` }}
                ></span>
              </li>
            ))}
          </ul>
        )}
      </div>
    </div>
  );
}
```

---

## Створіть файл `HighScores.css`

```css
/* HighScores.css */
.high-scores {
  max-width: 800px;
  margin: 40px auto;
  padding: 40px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-radius: 20px;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
  color: #fff;
}

.title {
  text-align: center;
  font-size: 36px;
  margin-bottom: 30px;
  font-weight: bold;
  color: #fff;
}

/* Статистика */
.statistics {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
  gap: 15px;
  margin-bottom: 30px;
}

.stat-card {
  background-color: rgba(255, 255, 255, 0.2);
  border: 2px solid rgba(255, 255, 255, 0.3);
  border-radius: 12px;
  padding: 20px;
  text-align: center;
  transition: all 0.3s ease;
}

.stat-card:hover {
  background-color: rgba(255, 255, 255, 0.3);
  transform: translateY(-5px);
}

.stat-label {
  display: block;
  font-size: 14px;
  margin-bottom: 8px;
  color: rgba(255, 255, 255, 0.9);
  font-weight: 500;
}

.stat-value {
  display: block;
  font-size: 32px;
  font-weight: bold;
  color: #fff;
}

/* Кнопки управління */
.controls {
  display: flex;
  flex-direction: column;
  gap: 15px;
  margin-bottom: 30px;
}

.btn {
  padding: 15px 25px;
  font-size: 16px;
  font-weight: bold;
  border: none;
  border-radius: 10px;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
}

.btn:hover:not(:disabled) {
  transform: translateY(-3px);
  box-shadow: 0 6px 15px rgba(0, 0, 0, 0.3);
}

.btn:active:not(:disabled) {
  transform: translateY(-1px);
}

.btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.btn-add {
  background-color: #27ae60;
  color: #fff;
}

.btn-add:hover:not(:disabled) {
  background-color: #229954;
}

.btn-remove {
  background-color: #e74c3c;
  color: #fff;
}

.btn-remove:hover:not(:disabled) {
  background-color: #c0392b;
}

.btn-increase {
  background-color: #3498db;
  color: #fff;
}

.btn-increase:hover:not(:disabled) {
  background-color: #2980b9;
}

/* Список балів */
.scores-container {
  background-color: rgba(255, 255, 255, 0.1);
  border-radius: 12px;
  padding: 25px;
  min-height: 300px;
}

.empty-message {
  text-align: center;
  font-size: 18px;
  color: rgba(255, 255, 255, 0.8);
  padding: 40px;
  font-style: italic;
}

.scores-list {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.score-item {
  display: flex;
  align-items: center;
  gap: 15px;
  padding: 15px 20px;
  background-color: rgba(255, 255, 255, 0.15);
  border: 2px solid rgba(255, 255, 255, 0.2);
  border-radius: 10px;
  position: relative;
  overflow: hidden;
  transition: all 0.3s ease;
}

.score-item:hover {
  background-color: rgba(255, 255, 255, 0.25);
  transform: translateX(5px);
}

.score-number {
  font-size: 16px;
  font-weight: bold;
  color: rgba(255, 255, 255, 0.7);
  min-width: 40px;
}

.score-value {
  font-size: 28px;
  font-weight: bold;
  color: #fff;
  min-width: 60px;
  z-index: 2;
}

.score-bar {
  position: absolute;
  left: 0;
  top: 0;
  height: 100%;
  background: linear-gradient(
    90deg,
    rgba(255, 255, 255, 0.2),
    rgba(255, 255, 255, 0.05)
  );
  transition: width 0.5s ease;
  z-index: 1;
}

/* Виділення мінімального та максимального балу */
.min-score {
  border-color: #e74c3c;
  background-color: rgba(231, 76, 60, 0.2);
}

.max-score {
  border-color: #f39c12;
  background-color: rgba(243, 156, 18, 0.2);
}

/* Адаптивність */
@media (max-width: 768px) {
  .high-scores {
    padding: 20px;
  }

  .title {
    font-size: 28px;
  }

  .statistics {
    grid-template-columns: repeat(2, 1fr);
  }

  .stat-value {
    font-size: 24px;
  }

  .score-value {
    font-size: 22px;
  }
}
```

---

## Використання в `App.jsx`

```jsx
// App.jsx
import "./App.css";
import HighScores from "./components/HighScores";

export default function App() {
  return (
    <div className="app">
      <HighScores />
    </div>
  );
}
```

### `App.css`

```css
/* App.css */
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  margin: 0;
  padding: 0;
  font-family: "Arial", sans-serif;
}

.app {
  min-height: 100vh;
  background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
  padding: 20px;
}
```

---

## Структура проєкту

```
src/
├── components/
│   ├── HighScores.jsx
│   └── HighScores.css
├── App.jsx
├── App.css
└── main.jsx
```

---

## Пояснення методів роботи з масивами ✅

### 1. **Spread оператор (`...`) для додавання**

```jsx
const addRandomScore = () => {
  const randomScore = Math.floor(Math.random() * 101);
  setScores([...scores, randomScore]); // Створюємо НОВИЙ масив
};
```

**Чому так:**

- `[...scores, randomScore]` створює новий масив з копією старих елементів + новий елемент
- React виявляє зміну стану (новий об'єкт масиву)
- **НЕ мутуємо** оригінальний масив

**❌ Неправильно:**

```jsx
scores.push(randomScore); // Мутує оригінальний масив!
setScores(scores); // React не побачить зміну
```

### 2. **`filter()` для видалення**

```jsx
const removeMinScore = () => {
  const minScore = Math.min(...scores);
  const minIndex = scores.indexOf(minScore);

  const newScores = scores.filter((score, index) => index !== minIndex);
  setScores(newScores);
};
```

**Як працює:**

- `filter()` створює **новий масив** з елементами, які пройшли перевірку
- Видаляємо елемент з індексом `minIndex`
- Залишаємо всі інші елементи

**Альтернатива (через slice):**

```jsx
const newScores = [...scores.slice(0, minIndex), ...scores.slice(minIndex + 1)];
```

### 3. **`map()` для оновлення всіх елементів**

```jsx
const increaseAllScores = () => {
  const increasedScores = scores.map((score) => score + 10);
  setScores(increasedScores);
};
```

**Як працює:**

- `map()` створює **новий масив** з трансформованими елементами
- Кожен елемент збільшується на 10
- Оригінальний масив не змінюється

---

## Додаткові методи (розширення функціоналу)

### Варіант 1: Скидання балів

```jsx
const resetScores = () => {
  setScores([]);
};

// У JSX:
<button className="btn btn-reset" onClick={resetScores}>
  🔄 Скинути всі бали
</button>;
```

### Варіант 2: Сортування балів

```jsx
const [sortOrder, setSortOrder] = useState("none"); // 'asc', 'desc', 'none'

const sortScores = () => {
  if (sortOrder === "none" || sortOrder === "desc") {
    const sorted = [...scores].sort((a, b) => a - b);
    setScores(sorted);
    setSortOrder("asc");
  } else {
    const sorted = [...scores].sort((a, b) => b - a);
    setScores(sorted);
    setSortOrder("desc");
  }
};

// У JSX:
<button className="btn btn-sort" onClick={sortScores}>
  🔀 Сортувати ({sortOrder === "asc" ? "↑" : sortOrder === "desc" ? "↓" : "-"})
</button>;
```

### Варіант 3: Видалення конкретного балу

```jsx
const removeScore = (index) => {
  const newScores = scores.filter((_, i) => i !== index);
  setScores(newScores);
};

// У JSX:
{
  scores.map((score, index) => (
    <li key={index} className="score-item">
      <span className="score-value">{score}</span>
      <button
        className="btn-small btn-delete"
        onClick={() => removeScore(index)}
      >
        ❌
      </button>
    </li>
  ));
}
```

---

## Корисні методи для роботи з масивами

```jsx
// 1. Знайти максимальний бал
const maxScore = Math.max(...scores);

// 2. Знайти мінімальний бал
const minScore = Math.min(...scores);

// 3. Обчислити середнє
const average = scores.reduce((sum, score) => sum + score, 0) / scores.length;

// 4. Відфільтрувати бали вище 80
const highScores = scores.filter((score) => score > 80);

// 5. Подвоїти всі бали
const doubled = scores.map((score) => score * 2);

// 6. Перевірити, чи є бал 100
const hasPerfectScore = scores.includes(100);

// 7. Знайти індекс першого балу > 90
const index = scores.findIndex((score) => score > 90);

// 8. Перевірити, чи всі бали > 50
const allPassed = scores.every((score) => score > 50);

// 9. Перевірити, чи є хоч один бал > 90
const someExcellent = scores.some((score) => score > 90);
```

---

## Запуск проєкту

```bash
npm run dev
```

**Тестування:**

1. Додайте кілька випадкових балів
2. Видаліть мінімальний бал
3. Збільште всі бали на 10
4. Спостерігайте за статистикою та візуалізацією

🎉 Готово!

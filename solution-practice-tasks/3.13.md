# Рішення: useRef для DOM та мутабельних значень

## Створіть файл `FocusControl.jsx`

```jsx
// FocusControl.jsx
import { useRef, useEffect, useState } from "react";
import "./FocusControl.css";

export default function FocusControl() {
  // 1. Ref для DOM-елемента (інпут)
  const inputRef = useRef(null);

  // 2. Стан для лічильника
  const [count, setCount] = useState(0);

  // 3. Ref для збереження попереднього значення лічильника
  const prevCountRef = useRef();

  // 4. Стан для значення інпута
  const [inputValue, setInputValue] = useState("");

  // Завдання 1: Автоматичний фокус при монтуванні
  useEffect(() => {
    // Фокусуємо інпут при першому рендері
    inputRef.current.focus();
    console.log("Компонент змонтовано: інпут отримав фокус");
  }, []); // Порожній масив = виконується тільки один раз

  // Завдання 2: Збереження попереднього значення count
  useEffect(() => {
    // Зберігаємо поточне значення count як попереднє
    // Це виконується ПІСЛЯ рендерингу, тому prevCountRef
    // міститиме значення з попереднього рендеру
    prevCountRef.current = count;
    console.log(`useEffect: Збережено попереднє значення: ${count}`);
  }, [count]); // Виконується при кожній зміні count

  // Обробник для кнопки "Фокус"
  const handleFocusClick = () => {
    inputRef.current.focus();
    console.log('Кнопка "Фокус": інпут отримав фокус');
  };

  // Обробник для збільшення лічильника
  const handleIncrement = () => {
    setCount(count + 1);
    console.log(`Збільшення: ${count} → ${count + 1}`);
  };

  // Обробник для зменшення лічильника
  const handleDecrement = () => {
    setCount(count - 1);
    console.log(`Зменшення: ${count} → ${count - 1}`);
  };

  // Обробник для скидання лічильника
  const handleReset = () => {
    setCount(0);
    console.log("Скидання лічильника до 0");
  };

  // Обробник для очищення інпута
  const handleClear = () => {
    setInputValue("");
    inputRef.current.focus();
    console.log("Інпут очищено та отримав фокус");
  };

  return (
    <div className="focus-control">
      <h1 className="title">🎯 Демонстрація useRef</h1>

      {/* Секція 1: Контроль фокусу */}
      <section className="section">
        <h2 className="section-title">📝 Завдання 1: Контроль фокусу</h2>
        <div className="input-group">
          <input
            ref={inputRef} // Прив'язуємо ref до інпута
            type="text"
            className="text-input"
            placeholder="Введіть текст..."
            value={inputValue}
            onChange={(e) => setInputValue(e.target.value)}
          />
          <button className="btn btn-focus" onClick={handleFocusClick}>
            🎯 Фокус на інпуті
          </button>
          <button className="btn btn-clear" onClick={handleClear}>
            🗑️ Очистити
          </button>
        </div>
        <p className="info-text">
          💡 Інпут автоматично отримує фокус при завантаженні компонента
        </p>
      </section>

      {/* Секція 2: Лічильник з історією */}
      <section className="section">
        <h2 className="section-title">🔢 Завдання 2: Лічильник з історією</h2>

        <div className="counter-display">
          <div className="counter-card">
            <span className="counter-label">Попереднє значення:</span>
            <span className="counter-value prev">
              {prevCountRef.current !== undefined ? prevCountRef.current : "—"}
            </span>
          </div>

          <div className="counter-card current">
            <span className="counter-label">Поточне значення:</span>
            <span className="counter-value">{count}</span>
          </div>
        </div>

        <div className="counter-controls">
          <button className="btn btn-decrement" onClick={handleDecrement}>
            ➖ Зменшити
          </button>
          <button className="btn btn-reset" onClick={handleReset}>
            🔄 Скинути
          </button>
          <button className="btn btn-increment" onClick={handleIncrement}>
            ➕ Збільшити
          </button>
        </div>

        <p className="info-text">
          💡 prevCountRef зберігає попереднє значення без ре-рендеру
        </p>
      </section>

      {/* Пояснення */}
      <section className="section explanation">
        <h2 className="section-title">📚 Як це працює</h2>

        <div className="explanation-card">
          <h3>1️⃣ useRef для DOM</h3>
          <code>const inputRef = useRef(null);</code>
          <p>Зберігає посилання на DOM-елемент для прямого доступу</p>
          <ul>
            <li>inputRef.current.focus() — фокусує інпут</li>
            <li>inputRef.current.value — отримує значення</li>
            <li>Не викликає ре-рендер при зміні</li>
          </ul>
        </div>

        <div className="explanation-card">
          <h3>2️⃣ useRef для мутабельного значення</h3>
          <code>const prevCountRef = useRef();</code>
          <p>Зберігає значення між рендерами без ре-рендеру</p>
          <ul>
            <li>prevCountRef.current — доступ до значення</li>
            <li>Зміна .current не викликає ре-рендер</li>
            <li>Ідеально для історії, таймерів, підписок</li>
          </ul>
        </div>

        <div className="explanation-card">
          <h3>3️⃣ Порядок виконання</h3>
          <ol>
            <li>Натискаємо кнопку "Збільшити"</li>
            <li>setCount(count + 1) → новий рендер</li>
            <li>Компонент рендериться з новим count</li>
            <li>useEffect виконується ПІСЛЯ рендеру</li>
            <li>
              prevCountRef.current = count (зберігаємо поточне як попереднє)
            </li>
          </ol>
        </div>
      </section>
    </div>
  );
}
```

---

## Створіть файл `FocusControl.css`

```css
/* FocusControl.css */
.focus-control {
  max-width: 900px;
  margin: 40px auto;
  padding: 40px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-radius: 20px;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
  color: #fff;
}

.title {
  text-align: center;
  font-size: 36px;
  margin-bottom: 40px;
  font-weight: bold;
  color: #fff;
}

/* Секції */
.section {
  background-color: rgba(255, 255, 255, 0.15);
  border: 2px solid rgba(255, 255, 255, 0.3);
  border-radius: 16px;
  padding: 30px;
  margin-bottom: 30px;
}

.section-title {
  font-size: 28px;
  margin-bottom: 20px;
  color: #fff;
  font-weight: bold;
}

/* Завдання 1: Контроль фокусу */
.input-group {
  display: flex;
  gap: 10px;
  margin-bottom: 15px;
  flex-wrap: wrap;
}

.text-input {
  flex: 1;
  min-width: 200px;
  padding: 15px 20px;
  font-size: 16px;
  border: 3px solid rgba(255, 255, 255, 0.3);
  border-radius: 10px;
  background-color: rgba(255, 255, 255, 0.9);
  color: #2c3e50;
  outline: none;
  transition: all 0.3s ease;
}

.text-input:focus {
  border-color: #ffd700;
  background-color: #fff;
  box-shadow: 0 0 0 4px rgba(255, 215, 0, 0.3);
  transform: scale(1.02);
}

/* Кнопки */
.btn {
  padding: 15px 25px;
  font-size: 16px;
  font-weight: bold;
  border: none;
  border-radius: 10px;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
}

.btn:hover {
  transform: translateY(-3px);
  box-shadow: 0 6px 15px rgba(0, 0, 0, 0.3);
}

.btn:active {
  transform: translateY(-1px);
}

.btn-focus {
  background-color: #3498db;
  color: #fff;
}

.btn-focus:hover {
  background-color: #2980b9;
}

.btn-clear {
  background-color: #e74c3c;
  color: #fff;
}

.btn-clear:hover {
  background-color: #c0392b;
}

/* Завдання 2: Лічильник */
.counter-display {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 20px;
  margin-bottom: 25px;
}

.counter-card {
  background-color: rgba(255, 255, 255, 0.2);
  border: 3px solid rgba(255, 255, 255, 0.3);
  border-radius: 12px;
  padding: 25px;
  text-align: center;
  transition: all 0.3s ease;
}

.counter-card:hover {
  background-color: rgba(255, 255, 255, 0.3);
  transform: translateY(-5px);
}

.counter-card.current {
  border-color: #ffd700;
  background-color: rgba(255, 215, 0, 0.2);
}

.counter-label {
  display: block;
  font-size: 16px;
  margin-bottom: 10px;
  color: rgba(255, 255, 255, 0.9);
}

.counter-value {
  display: block;
  font-size: 56px;
  font-weight: bold;
  color: #fff;
  text-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
}

.counter-value.prev {
  color: rgba(255, 255, 255, 0.7);
}

.counter-controls {
  display: flex;
  gap: 15px;
  margin-bottom: 15px;
  flex-wrap: wrap;
}

.counter-controls .btn {
  flex: 1;
  min-width: 120px;
}

.btn-increment {
  background-color: #27ae60;
  color: #fff;
}

.btn-increment:hover {
  background-color: #229954;
}

.btn-decrement {
  background-color: #e67e22;
  color: #fff;
}

.btn-decrement:hover {
  background-color: #d35400;
}

.btn-reset {
  background-color: #95a5a6;
  color: #fff;
}

.btn-reset:hover {
  background-color: #7f8c8d;
}

/* Інформаційний текст */
.info-text {
  font-size: 14px;
  color: rgba(255, 255, 255, 0.8);
  font-style: italic;
  text-align: center;
  margin: 0;
}

/* Пояснення */
.explanation {
  background-color: rgba(255, 255, 255, 0.1);
}

.explanation-card {
  background-color: rgba(0, 0, 0, 0.2);
  border-radius: 10px;
  padding: 20px;
  margin-bottom: 20px;
}

.explanation-card:last-child {
  margin-bottom: 0;
}

.explanation-card h3 {
  margin-top: 0;
  margin-bottom: 12px;
  font-size: 22px;
  color: #ffd700;
}

.explanation-card code {
  display: block;
  background-color: rgba(0, 0, 0, 0.4);
  padding: 12px;
  border-radius: 6px;
  font-family: "Courier New", monospace;
  font-size: 14px;
  color: #a8e6cf;
  margin: 10px 0;
  overflow-x: auto;
}

.explanation-card p {
  margin-bottom: 12px;
  line-height: 1.6;
}

.explanation-card ul,
.explanation-card ol {
  margin-left: 20px;
  line-height: 1.8;
}

.explanation-card li {
  margin-bottom: 8px;
}

/* Адаптивність */
@media (max-width: 768px) {
  .focus-control {
    padding: 20px;
  }

  .title {
    font-size: 28px;
  }

  .section-title {
    font-size: 22px;
  }

  .input-group {
    flex-direction: column;
  }

  .text-input {
    min-width: 100%;
  }

  .counter-value {
    font-size: 42px;
  }

  .counter-controls {
    flex-direction: column;
  }

  .counter-controls .btn {
    width: 100%;
  }
}
```

---

## Використання в `App.jsx`

```jsx
// App.jsx
import "./App.css";
import FocusControl from "./components/FocusControl";

export default function App() {
  return (
    <div className="app">
      <FocusControl />
    </div>
  );
}
```

### `App.css`

```css
/* App.css */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  margin: 0;
  padding: 0;
  font-family: "Arial", sans-serif;
}

.app {
  min-height: 100vh;
  background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
  padding: 20px;
}
```

---

## Структура проєкту

```
src/
├── components/
│   ├── FocusControl.jsx
│   └── FocusControl.css
├── App.jsx
├── App.css
└── main.jsx
```

---

## Пояснення useRef ✅

### **Що таке useRef?**

`useRef` — це хук, який повертає мутабельний об'єкт з властивістю `.current`. Значення зберігається між рендерами і **не викликає ре-рендер** при зміні.

### **Синтаксис:**

```jsx
const ref = useRef(initialValue);
```

**Де:**

- `initialValue` — початкове значення `ref.current`
- `ref.current` — мутабельне значення

---

## Два основні використання useRef

### 1. **Доступ до DOM-елементів**

```jsx
const inputRef = useRef(null);

// Прив'язка до елемента
<input ref={inputRef} type="text" />;

// Використання
inputRef.current.focus(); // Фокус
inputRef.current.value; // Значення
inputRef.current.blur(); // Втрата фокусу
```

**Коли використовувати:**

- Керування фокусом
- Вимірювання розмірів елемента
- Інтеграція зі сторонніми бібліотеками
- Анімації

### 2. **Збереження мутабельних значень**

```jsx
const prevValueRef = useRef();

useEffect(() => {
  prevValueRef.current = value; // Зберігаємо без ре-рендеру
}, [value]);
```

**Коли використовувати:**

- Збереження попередніх значень
- Зберігання таймерів/інтервалів
- Лічильники рендерів
- Прапорці (flags)

---

## useRef vs useState

### **useState** — викликає ре-рендер

```jsx
const [count, setCount] = useState(0);

setCount(1); // Викликає ре-рендер ✅
```

**Використання:**

- Дані для відображення в UI
- Стан, який змінює інтерфейс

### **useRef** — НЕ викликає ре-рендер

```jsx
const countRef = useRef(0);

countRef.current = 1; // НЕ викликає ре-рендер ✅
```

**Використання:**

- Дані, які не впливають на UI
- DOM-посилання
- Таймери, підписки

---

## Приклади використання

### Приклад 1: Таймер без ре-рендеру

```jsx
function Timer() {
  const [seconds, setSeconds] = useState(0);
  const intervalRef = useRef(null);

  const startTimer = () => {
    intervalRef.current = setInterval(() => {
      setSeconds((s) => s + 1);
    }, 1000);
  };

  const stopTimer = () => {
    clearInterval(intervalRef.current);
  };

  useEffect(() => {
    return () => clearInterval(intervalRef.current); // Cleanup
  }, []);

  return (
    <div>
      <p>Секунд: {seconds}</p>
      <button onClick={startTimer}>Старт</button>
      <button onClick={stopTimer}>Стоп</button>
    </div>
  );
}
```

### Приклад 2: Лічильник рендерів

```jsx
function RenderCounter() {
  const [count, setCount] = useState(0);
  const renderCount = useRef(0);

  useEffect(() => {
    renderCount.current += 1; // Не викликає ре-рендер
  });

  return (
    <div>
      <p>Count: {count}</p>
      <p>Кількість рендерів: {renderCount.current}</p>
      <button onClick={() => setCount(count + 1)}>Increment</button>
    </div>
  );
}
```

### Приклад 3: Попереднє значення

```jsx
function usePrevious(value) {
  const ref = useRef();

  useEffect(() => {
    ref.current = value;
  }, [value]);

  return ref.current;
}

// Використання
function Component() {
  const [count, setCount] = useState(0);
  const prevCount = usePrevious(count);

  return (
    <div>
      <p>Поточне: {count}</p>
      <p>Попереднє: {prevCount}</p>
      <button onClick={() => setCount(count + 1)}>+</button>
    </div>
  );
}
```

---

## Важливі моменти ⚠️

### 1. **Не читайте/пишіть ref.current під час рендерингу**

```jsx
// ❌ НЕПРАВИЛЬНО
function Component() {
  const ref = useRef(0);
  ref.current += 1; // Не робіть так під час рендерингу!

  return <div>{ref.current}</div>;
}

// ✅ ПРАВИЛЬНО
function Component() {
  const ref = useRef(0);

  useEffect(() => {
    ref.current += 1; // Робіть це в useEffect або обробниках
  });

  return <div>{ref.current}</div>;
}
```

### 2. **ref.current мутабельний**

```jsx
// Можна змінювати напряму
ref.current = newValue; // ✅
```

### 3. **useRef НЕ для стану UI**

```jsx
// ❌ НЕПРАВИЛЬНО — UI не оновиться
const countRef = useRef(0);
countRef.current += 1;

// ✅ ПРАВИЛЬНО — використовуйте useState
const [count, setCount] = useState(0);
setCount(count + 1);
```

---

## Запуск проєкту

```bash
npm run dev
```

**Тестування:**

1. Відкрийте консоль браузера (F12 → Console)
2. Спостерігайте за автоматичним фокусом на інпуті
3. Натисніть кнопку "Фокус" — інпут знову отримає фокус
4. Змінюйте лічильник — спостерігайте за попереднім значенням
5. У консолі побачите логи про зміни

🎉 Готово!

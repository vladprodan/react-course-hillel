# Рішення: React Hook Form з Yup валідацією

## Крок 1: Встановлення необхідних пакетів

Встановіть Yup та resolvers:

```bash
npm install yup @hookform/resolvers
```

---

## Крок 2: Створення схеми валідації

Створіть файл `validationSchemas.js` у папці `src/utils` або `src/schemas`:

```javascript
import * as yup from "yup";

// Схема для форми профілю
export const profileSchema = yup.object().shape({
  username: yup
    .string()
    .required("Ім'я користувача є обов'язковим")
    .min(3, "Мінімум 3 символи")
    .max(20, "Максимум 20 символів"),

  email: yup
    .string()
    .required("Email є обов'язковим")
    .email("Невірний формат email"),

  age: yup
    .number()
    .required("Вік є обов'язковим")
    .typeError("Вік має бути числом")
    .integer("Вік має бути цілим числом")
    .min(18, "Мінімальний вік - 18 років")
    .max(120, "Максимальний вік - 120 років"),

  acceptTerms: yup
    .boolean()
    .required("Необхідно прийняти умови")
    .oneOf([true], "Ви повинні прийняти умови використання"),
});

// Розширена схема з додатковими полями
export const extendedProfileSchema = yup.object().shape({
  firstName: yup
    .string()
    .required("Ім'я є обов'язковим")
    .min(2, "Мінімум 2 символи")
    .matches(/^[a-zA-Zа-яА-ЯіІїЇєЄ]+$/, "Тільки літери"),

  lastName: yup
    .string()
    .required("Прізвище є обов'язковим")
    .min(2, "Мінімум 2 символи")
    .matches(/^[a-zA-Zа-яА-ЯіІїЇєЄ]+$/, "Тільки літери"),

  username: yup
    .string()
    .required("Ім'я користувача є обов'язковим")
    .min(3, "Мінімум 3 символи")
    .max(20, "Максимум 20 символів")
    .matches(/^[a-zA-Z0-9_]+$/, "Тільки латинські літери, цифри та _"),

  email: yup
    .string()
    .required("Email є обов'язковим")
    .email("Невірний формат email"),

  phone: yup
    .string()
    .required("Телефон є обов'язковим")
    .matches(/^\+380\d{9}$/, "Формат: +380XXXXXXXXX"),

  age: yup
    .number()
    .required("Вік є обов'язковим")
    .typeError("Вік має бути числом")
    .integer("Вік має бути цілим числом")
    .min(18, "Мінімальний вік - 18 років")
    .max(120, "Максимальний вік - 120 років"),

  website: yup.string().url("Невірний формат URL").nullable(),

  bio: yup.string().max(500, "Максимум 500 символів").nullable(),

  password: yup
    .string()
    .required("Пароль є обов'язковим")
    .min(8, "Мінімум 8 символів")
    .matches(
      /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])/,
      "Пароль має містити великі, малі літери, цифру та спецсимвол"
    ),

  confirmPassword: yup
    .string()
    .required("Підтвердження пароля є обов'язковим")
    .oneOf([yup.ref("password")], "Паролі не співпадають"),

  acceptTerms: yup
    .boolean()
    .required("Необхідно прийняти умови")
    .oneOf([true], "Ви повинні прийняти умови використання"),

  newsletter: yup.boolean(),
});
```

---

## Крок 3: Створення базової форми ProfileForm

Створіть файл `ProfileForm.jsx` у папці `src/components`:

```jsx
import React from "react";
import { useForm } from "react-hook-form";
import { yupResolver } from "@hookform/resolvers/yup";
import { profileSchema } from "../utils/validationSchemas";

const ProfileForm = () => {
  // Ініціалізація useForm з yupResolver
  const {
    register,
    handleSubmit,
    formState: { errors, isSubmitting, isValid },
  } = useForm({
    resolver: yupResolver(profileSchema),
    mode: "onChange", // Валідація при кожній зміні
  });

  // Обробник відправки форми
  const onSubmit = async (data) => {
    console.log("✅ Дані форми валідні:", data);

    // Імітація API запиту
    await new Promise((resolve) => setTimeout(resolve, 1000));

    alert(
      `Профіль оновлено!\n\nІм'я: ${data.username}\nEmail: ${data.email}\nВік: ${data.age}`
    );
  };

  return (
    <div className="profile-form-container">
      <div className="form-card">
        <h2>👤 Оновлення Профілю</h2>
        <p className="form-subtitle">Валідація через Yup схему</p>

        <form onSubmit={handleSubmit(onSubmit)} className="profile-form">
          {/* Ім'я користувача */}
          <div className="form-field">
            <label htmlFor="username">
              Ім'я користувача <span className="required">*</span>
            </label>
            <input
              id="username"
              type="text"
              placeholder="Введіть ім'я користувача"
              {...register("username")}
              className={errors.username ? "input-error" : ""}
            />
            {errors.username && (
              <span className="error-message">
                ⚠️ {errors.username.message}
              </span>
            )}
          </div>

          {/* Email */}
          <div className="form-field">
            <label htmlFor="email">
              Email <span className="required">*</span>
            </label>
            <input
              id="email"
              type="email"
              placeholder="your.email@example.com"
              {...register("email")}
              className={errors.email ? "input-error" : ""}
            />
            {errors.email && (
              <span className="error-message">⚠️ {errors.email.message}</span>
            )}
          </div>

          {/* Вік */}
          <div className="form-field">
            <label htmlFor="age">
              Вік <span className="required">*</span>
            </label>
            <input
              id="age"
              type="number"
              placeholder="Введіть ваш вік"
              {...register("age")}
              className={errors.age ? "input-error" : ""}
            />
            {errors.age && (
              <span className="error-message">⚠️ {errors.age.message}</span>
            )}
          </div>

          {/* Прийняття умов */}
          <div className="form-field checkbox-field">
            <label className="checkbox-label">
              <input type="checkbox" {...register("acceptTerms")} />
              <span>Я приймаю умови використання</span>
            </label>
            {errors.acceptTerms && (
              <span className="error-message">
                ⚠️ {errors.acceptTerms.message}
              </span>
            )}
          </div>

          {/* Кнопка відправки */}
          <button
            type="submit"
            className="submit-button"
            disabled={isSubmitting}
          >
            {isSubmitting ? "Збереження..." : "Оновити профіль"}
          </button>
        </form>

        {/* Інформаційний блок */}
        <div className="yup-info">
          <h4>🔍 Правила валідації (Yup схема):</h4>
          <ul>
            <li>
              <strong>username:</strong> мінімум 3 символи
            </li>
            <li>
              <strong>email:</strong> коректний формат email
            </li>
            <li>
              <strong>age:</strong> ціле число, мінімум 18
            </li>
            <li>
              <strong>acceptTerms:</strong> має бути true
            </li>
          </ul>
        </div>
      </div>
    </div>
  );
};

export default ProfileForm;
```

---

## Крок 4: Створення розширеної форми

Створіть файл `ExtendedProfileForm.jsx` у папці `src/components`:

```jsx
import React, { useState } from "react";
import { useForm } from "react-hook-form";
import { yupResolver } from "@hookform/resolvers/yup";
import { extendedProfileSchema } from "../utils/validationSchemas";

const ExtendedProfileForm = () => {
  const [showPassword, setShowPassword] = useState(false);
  const [submitSuccess, setSubmitSuccess] = useState(false);

  const {
    register,
    handleSubmit,
    watch,
    reset,
    formState: { errors, isSubmitting, isValid, isDirty },
  } = useForm({
    resolver: yupResolver(extendedProfileSchema),
    mode: "onChange",
    defaultValues: {
      firstName: "",
      lastName: "",
      username: "",
      email: "",
      phone: "",
      age: "",
      website: "",
      bio: "",
      password: "",
      confirmPassword: "",
      acceptTerms: false,
      newsletter: false,
    },
  });

  // Спостереження за паролем для індикатора сили
  const password = watch("password");

  // Функція перевірки сили пароля
  const getPasswordStrength = (pwd) => {
    if (!pwd) return { strength: 0, label: "", color: "#e0e0e0" };

    let strength = 0;
    if (pwd.length >= 8) strength++;
    if (/[a-z]/.test(pwd)) strength++;
    if (/[A-Z]/.test(pwd)) strength++;
    if (/\d/.test(pwd)) strength++;
    if (/[@$!%*?&]/.test(pwd)) strength++;

    const levels = [
      { strength: 1, label: "Дуже слабкий", color: "#f44336" },
      { strength: 2, label: "Слабкий", color: "#ff9800" },
      { strength: 3, label: "Середній", color: "#ffc107" },
      { strength: 4, label: "Хороший", color: "#8bc34a" },
      { strength: 5, label: "Відмінний", color: "#4caf50" },
    ];

    return levels[strength - 1] || levels[0];
  };

  const passwordStrength = getPasswordStrength(password);

  const onSubmit = async (data) => {
    console.log("✅ Валідні дані профілю:", data);

    // Імітація API запиту
    await new Promise((resolve) => setTimeout(resolve, 1500));

    setSubmitSuccess(true);

    // Скидання форми після успіху
    setTimeout(() => {
      setSubmitSuccess(false);
      reset();
    }, 3000);
  };

  return (
    <div className="profile-form-container">
      <div className="form-card extended">
        <h2>🚀 Розширений Профіль</h2>
        <p className="form-subtitle">Повна валідація з Yup схемою</p>

        {submitSuccess && (
          <div className="success-banner">✅ Профіль успішно оновлено!</div>
        )}

        <form onSubmit={handleSubmit(onSubmit)} className="profile-form">
          {/* Ім'я та Прізвище в одному рядку */}
          <div className="form-row">
            <div className="form-field">
              <label htmlFor="firstName">
                Ім'я <span className="required">*</span>
              </label>
              <input
                id="firstName"
                type="text"
                placeholder="Іван"
                {...register("firstName")}
                className={errors.firstName ? "input-error" : ""}
              />
              {errors.firstName && (
                <span className="error-message">
                  {errors.firstName.message}
                </span>
              )}
            </div>

            <div className="form-field">
              <label htmlFor="lastName">
                Прізвище <span className="required">*</span>
              </label>
              <input
                id="lastName"
                type="text"
                placeholder="Петренко"
                {...register("lastName")}
                className={errors.lastName ? "input-error" : ""}
              />
              {errors.lastName && (
                <span className="error-message">{errors.lastName.message}</span>
              )}
            </div>
          </div>

          {/* Username */}
          <div className="form-field">
            <label htmlFor="username">
              Ім'я користувача <span className="required">*</span>
            </label>
            <input
              id="username"
              type="text"
              placeholder="ivan_petrenko"
              {...register("username")}
              className={errors.username ? "input-error" : ""}
            />
            {errors.username && (
              <span className="error-message">{errors.username.message}</span>
            )}
            <span className="field-hint">
              Тільки латинські літери, цифри та підкреслення
            </span>
          </div>

          {/* Email та Телефон */}
          <div className="form-row">
            <div className="form-field">
              <label htmlFor="email">
                Email <span className="required">*</span>
              </label>
              <input
                id="email"
                type="email"
                placeholder="ivan@example.com"
                {...register("email")}
                className={errors.email ? "input-error" : ""}
              />
              {errors.email && (
                <span className="error-message">{errors.email.message}</span>
              )}
            </div>

            <div className="form-field">
              <label htmlFor="phone">
                Телефон <span className="required">*</span>
              </label>
              <input
                id="phone"
                type="tel"
                placeholder="+380123456789"
                {...register("phone")}
                className={errors.phone ? "input-error" : ""}
              />
              {errors.phone && (
                <span className="error-message">{errors.phone.message}</span>
              )}
            </div>
          </div>

          {/* Вік та Веб-сайт */}
          <div className="form-row">
            <div className="form-field">
              <label htmlFor="age">
                Вік <span className="required">*</span>
              </label>
              <input
                id="age"
                type="number"
                placeholder="25"
                {...register("age")}
                className={errors.age ? "input-error" : ""}
              />
              {errors.age && (
                <span className="error-message">{errors.age.message}</span>
              )}
            </div>

            <div className="form-field">
              <label htmlFor="website">Веб-сайт</label>
              <input
                id="website"
                type="url"
                placeholder="https://example.com"
                {...register("website")}
                className={errors.website ? "input-error" : ""}
              />
              {errors.website && (
                <span className="error-message">{errors.website.message}</span>
              )}
            </div>
          </div>

          {/* Біографія */}
          <div className="form-field">
            <label htmlFor="bio">Про себе</label>
            <textarea
              id="bio"
              rows="4"
              placeholder="Розкажіть про себе..."
              {...register("bio")}
              className={errors.bio ? "input-error" : ""}
            />
            {errors.bio && (
              <span className="error-message">{errors.bio.message}</span>
            )}
            <span className="field-hint">Максимум 500 символів</span>
          </div>

          {/* Пароль */}
          <div className="form-field">
            <label htmlFor="password">
              Пароль <span className="required">*</span>
            </label>
            <div className="password-input-wrapper">
              <input
                id="password"
                type={showPassword ? "text" : "password"}
                placeholder="Введіть пароль"
                {...register("password")}
                className={errors.password ? "input-error" : ""}
              />
              <button
                type="button"
                onClick={() => setShowPassword(!showPassword)}
                className="toggle-password"
              >
                {showPassword ? "🙈" : "👁️"}
              </button>
            </div>
            {errors.password && (
              <span className="error-message">{errors.password.message}</span>
            )}

            {/* Індикатор сили пароля */}
            {password && (
              <div className="password-strength">
                <div className="strength-bar">
                  <div
                    className="strength-fill"
                    style={{
                      width: `${(passwordStrength.strength / 5) * 100}%`,
                      backgroundColor: passwordStrength.color,
                    }}
                  />
                </div>
                <span style={{ color: passwordStrength.color }}>
                  {passwordStrength.label}
                </span>
              </div>
            )}
          </div>

          {/* Підтвердження пароля */}
          <div className="form-field">
            <label htmlFor="confirmPassword">
              Підтвердження пароля <span className="required">*</span>
            </label>
            <input
              id="confirmPassword"
              type={showPassword ? "text" : "password"}
              placeholder="Повторіть пароль"
              {...register("confirmPassword")}
              className={errors.confirmPassword ? "input-error" : ""}
            />
            {errors.confirmPassword && (
              <span className="error-message">
                {errors.confirmPassword.message}
              </span>
            )}
          </div>

          {/* Checkboxes */}
          <div className="form-field checkbox-field">
            <label className="checkbox-label">
              <input type="checkbox" {...register("acceptTerms")} />
              <span>
                Я приймаю умови використання <span className="required">*</span>
              </span>
            </label>
            {errors.acceptTerms && (
              <span className="error-message">
                {errors.acceptTerms.message}
              </span>
            )}
          </div>

          <div className="form-field checkbox-field">
            <label className="checkbox-label">
              <input type="checkbox" {...register("newsletter")} />
              <span>Підписатися на розсилку новин</span>
            </label>
          </div>

          {/* Кнопка відправки */}
          <button
            type="submit"
            className="submit-button"
            disabled={isSubmitting || !isValid || !isDirty}
          >
            {isSubmitting ? "Збереження..." : "Зберегти зміни"}
          </button>
        </form>

        {/* Технічна інформація */}
        <div className="validation-details">
          <h4>📊 Стан форми:</h4>
          <div className="status-grid">
            <div className="status-item">
              <span className="status-label">Валідна:</span>
              <span className={`status-value ${isValid ? "valid" : "invalid"}`}>
                {isValid ? "✓ Так" : "✗ Ні"}
              </span>
            </div>
            <div className="status-item">
              <span className="status-label">Змінена:</span>
              <span className={`status-value ${isDirty ? "valid" : "invalid"}`}>
                {isDirty ? "✓ Так" : "✗ Ні"}
              </span>
            </div>
            <div className="status-item">
              <span className="status-label">Відправляється:</span>
              <span
                className={`status-value ${isSubmitting ? "valid" : "invalid"}`}
              >
                {isSubmitting ? "✓ Так" : "✗ Ні"}
              </span>
            </div>
            <div className="status-item">
              <span className="status-label">Помилок:</span>
              <span className="status-value">{Object.keys(errors).length}</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ExtendedProfileForm;
```

---

## Крок 5: Додавання стилів

Додайте до `App.css`:

```css
/* Форма профілю */
.profile-form-container {
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 700px;
  padding: 40px 20px;
}

.form-card.extended {
  max-width: 800px;
}

.profile-form {
  display: flex;
  flex-direction: column;
  gap: 20px;
}

/* Рядок з двома полями */
.form-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
}

/* Textarea */
textarea {
  width: 100%;
  padding: 14px 16px;
  border: 2px solid #e0e0e0;
  border-radius: 8px;
  font-size: 16px;
  font-family: inherit;
  transition: all 0.3s ease;
  resize: vertical;
}

textarea:focus {
  outline: none;
  border-color: #667eea;
  box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
}

/* Індикатор сили пароля */
.password-strength {
  margin-top: 10px;
  display: flex;
  align-items: center;
  gap: 12px;
}

.strength-bar {
  flex: 1;
  height: 6px;
  background: #e0e0e0;
  border-radius: 3px;
  overflow: hidden;
}

.strength-fill {
  height: 100%;
  transition: all 0.3s ease;
  border-radius: 3px;
}

.password-strength span {
  font-size: 13px;
  font-weight: 600;
}

/* Yup інформація */
.yup-info {
  margin-top: 25px;
  padding: 20px;
  background: linear-gradient(135deg, #667eea10 0%, #764ba210 100%);
  border-radius: 10px;
  border: 2px solid #667eea30;
}

.yup-info h4 {
  color: #333;
  margin-bottom: 15px;
  font-size: 16px;
}

.yup-info ul {
  list-style: none;
  padding: 0;
  margin: 0;
}

.yup-info li {
  color: #666;
  font-size: 14px;
  padding: 8px 0;
  padding-left: 25px;
  position: relative;
}

.yup-info li::before {
  content: "✓";
  position: absolute;
  left: 0;
  color: #667eea;
  font-weight: bold;
  font-size: 16px;
}

/* Деталі валідації */
.validation-details {
  margin-top: 30px;
  padding: 25px;
  background: #f9f9f9;
  border-radius: 12px;
}

.validation-details h4 {
  color: #333;
  margin-bottom: 20px;
  font-size: 16px;
}

.status-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
  gap: 15px;
}

.status-item {
  background: white;
  padding: 15px;
  border-radius: 8px;
  border: 2px solid #e0e0e0;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.status-label {
  font-size: 13px;
  color: #999;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.status-value {
  font-size: 18px;
  font-weight: bold;
  color: #333;
}

.status-value.valid {
  color: #4caf50;
}

.status-value.invalid {
  color: #f44336;
}

/* Адаптивність */
@media (max-width: 768px) {
  .form-row {
    grid-template-columns: 1fr;
  }

  .status-grid {
    grid-template-columns: 1fr 1fr;
  }
}
```

---

## Крок 6: Інтеграція в App.jsx

```jsx
import ProfileForm from './components/ProfileForm';
import ExtendedProfileForm from './components/ExtendedProfileForm';

// В Routes:
<Route path="/profile" element={<ProfileForm />} />
<Route path="/profile-extended" element={<ExtendedProfileForm />} />
```

---

## Крок 7: Оновлення Navigation.jsx

```jsx
<li>
  <NavLink
    to="/profile"
    className={({ isActive }) => isActive ? 'active-link' : 'default-link'}
  >
    👤 Профіль (Yup)
  </NavLink>
</li>
<li>
  <NavLink
    to="/profile-extended"
    className={({ isActive }) => isActive ? 'active-link' : 'default-link'}
  >
    🎯 Розширений профіль
  </NavLink>
</li>
```

---

## Крок 8: Тестування

```bash
npm run dev
```

### Сценарії:

**Тест 1:** Username менше 3 символів → помилка  
**Тест 2:** Невалідний email → помилка  
**Тест 3:** Вік менше 18 → помилка  
**Тест 4:** Checkbox не відмічено → помилка  
**Тест 5:** Всі поля валідні → успішна відправка

---

## Результат

✅ **Yup Resolver** - Використано yupResolver для підключення схеми  
✅ **Декларативна валідація** - Всі правила в Yup схемі  
✅ **Обробка даних** - onSubmit викликається тільки при валідності  
✅ **Типи полів** - age (number), acceptTerms (checkbox)

---

## Переваги Yup

- 📦 Централізована валідація
- 🔄 Повторне використання схем
- 🎯 TypeScript підтримка
- ✨ Читабельний код
- 🛡️ Потужні правила валідації
